# Handoff: mpu мини-Smalltalk — состояние на 2026-09-23 вечер

Хост (эта роль) — сессия «vm», tmux `w:@38.%38`, каталог `~/mr/mp/mpu`.
Исполнитель — изолированная Deno-сессия `mpu-157`, tmux `w:@40.%40`, каталог
`~/mr/mp/mpu/ts`, стоит на `wait task` (ячейка — `# ОТЧЁТ` по 167b).

**Остановка:** владелец велел после 167b стоять и ждать «go». Ничего не
постить до «go».

## Сделано и установлено (всё проверено живьём, кроме помеченного)

| Порция | Что | Спека |
|---|---|---|
| 158–165, 165b | ключи, форматы, отбор, `it`, дверь `ask`, варианты, отказ-объект, описание тула v5 | `platform/*.md` |
| 165c | stdin по запросу строки | `stdin-on-request.md` |
| 165d | логи страницами, `limit:`, коллекция строк, большой ответ MCP файлом `/tmp/mpu-out` | `long-output.md` |
| 166a | исполнитель строк `mpu-worker`, пул, сторож памяти, `MemoryMax=80%` (настоящая нехватка памяти не вызывалась) | `line-executor.md` |
| 166 | вычислитель: `.`, `:=`, `@x`, блоки, базовые объекты, уступка, стек в куче | `evaluator.md` |
| 166b | `record`/`items` — отбор видит то, что печатает `end json` | `result-record.md` |
| 167 | `ask` в составной строке: обход до исполнения, вопрос на каждую запись (вопросы живьём не снимались — прод) | `ask-composite.md` |
| 168 | образ `image.db`: `define:`/`forget:`, метод как команда | `image.md` |
| 167b | ключ-текст берёт слово как есть, `^…^` с удвоением `^^` (починка telegram через MCP) | `at-word-literal.md` |

Все спеки — `mpu/ts/docs/specs/` (платформенные — `platform/`). Последний
коммит приёмки — `3e28dc8`. `mpu` запушен до `047f8de`; дальше — не пушено
(пушить только по просьбе).

## Очередь после «go» (постановки готовы в `~/tmp/prompt-*.txt`)

1. **167c** — окно подтверждения MCP только Accept/Decline (`prompt-167c.txt`;
   спека `mcp-objects.md`, голден `fixtures/mcp-objects/elicitation.json` —
   ответ `accept` с пустым `content` — догадка, после установки снять живой).
2. **169** — `image sync` (`prompt-169.txt`, спека `specs/image-sync.md`);
   первым пунктом досыл 168: строка `define:` не пишется в журнал вызовов.
3. **170** — программа из stdin и `run:` (`prompt-170.txt`, `program-input.md`).
4. После 170 — слепая приёмка языка: sonnet на `docs/probes/2026-09-22-blocks/desc-v9.txt`,
   задачи `tasks-v8.txt`, исполнением на живой системе, порог 9/10.
5. **171** — web образа (`prompt-171.txt`, `specs/web-image.md`).
6. **172+** — описание тула и справки итерациями со слепым замером (порог 9/10
   на двух наборах подряд); в описание — пример `ask` первым словом при записи
   в блоке (оба агента замера 167b ошиблись по пропуску).
7. Установка, живая проба (терминал, MCP, web), правка корневого `mp/CLAUDE.md`
   (раздел mpu: ключ-текст, `^…^`/`^^`, программы, образ).
8. Оркестратор `mpu task` T1–T5 — `docs/2026-09-23-task-orchestrator-design.md`,
   план порций пишется при старте.

Мелочи из приёмок (в `~/tmp/mpu-smalltalk/questions.md`, взять порцией-досылом):
`grep-regex: ^ERROR` — нужен «сырой» вид ключа; `define:` разбирает `purpose:`
по старому правилу `^`; отказ разбора теряет `ask` в `hint`; `telegram search`
не объявляет `items`; группа команд значением (`x := kiten`) — решать замером.

## Процедура на каждую порцию (не менять)

1. Отчёт: фоновый `deno task handoff wait report 36000` (из `ts/`), затем
   `deno task handoff read > ~/tmp/mpu-smalltalk/report-<N>.md`.
2. Заморозка голденов: `cp` из `back/src/**/testdata/…` в `docs/specs/fixtures/…`,
   сначала `diff` — изменения ровно названные.
3. Гейты сам, по одному: `deno fmt --check`, `deno lint`, `deno check .`,
   `deno task test`, `deno task smoke`.
4. `bash install.sh </dev/null` (stdin закрыть!), живая проба — без записей в
   прод; `telegram send` — `allow`, не вызывать даже на `me` (пять тестовых
   сообщений ушли в «Избранное» 2026-09-23 — ошибка хоста).
5. Статус спеки «реализовано … принято …» с живыми числами, глоссарий, коммит
   `git -C ~/mr/mp/mpu` с путями поимённо.
6. Следующая: `bash ~/mr/mp/.claude/skills/proc-isolated-tmux-sessions/clear.sh
   w:@40.%40 ~/mr/mp/mpu/ts/.tmp/buf.txt`, дождаться простоя (нет «esc to
   interrupt»), `deno task handoff post ~/tmp/prompt-<N>.txt`, фоновое ожидание
   отчёта.
7. Вопросы исполнителя (SendMessage) — сперва правка спеки + коммит, потом
   ответ. Синтаксические развилки — слепой замер sonnet (правило
   `mpu/CLAUDE.md`); без явного перевеса — разумный вариант (владелец:
   обратная совместимость не нужна).

Генератор постановок: `~/tmp/mpu-smalltalk/brief.py` (`brief(n, title, why,
read, scen, design, offload, keep, bounds, muts, know)`); отчёты порций —
`~/tmp/mpu-smalltalk/report-*.md`.

## Ключевые решения владельца этой сессии

- Язык: всё объект и сообщение; синтаксис решает слепой замер sonnet;
  громкий отказ с готовой строкой; обратная совместимость не нужна.
- Длинные логи — работать и в MCP; нельзя целиком — файл.
- Ключ-текст решает тип значения; текст из нескольких слов — `^…^`, `^` внутри
  удвоением (замер 9/10 против 7/10 у `--`); вложенные кавычки отвергнуты.
- Окно подтверждения MCP — только Accept/Decline.
- Оркестратор `mpu task` — после всего языка и справки.
