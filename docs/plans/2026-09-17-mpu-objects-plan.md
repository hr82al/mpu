# mpu: объекты и живой бэкенд — план порций

> **Исполнение:** не субагентами основной сессии. Каждая порция — спека в
> `ts/docs/specs/` + постановка `~/tmp/prompt-<N>.txt`, которую оператор
> запускает в изолированной сессии (`mpu/CLAUDE.md`, «Роли сессий»). Код в
> плане не приводится: устройство проектирует исполнитель по чек-листам
> (`checklists/`), постановка его не выдаёт.

**Цель:** превратить один бинарь `mpu` в набор подпроектов вокруг живого
бэкенда с командами-сообщениями и политикой подтверждений, не ломая рабочий
`mpu` до переключения.

**Архитектура:** `ts/back` (Hono + JSON-RPC, объекты, политика, интеграции),
тонкие `ts/mcp`, `ts/cli`, `ts/complete`, `ts/supervisor`, фронт `ts/web`;
один юнит `mpu.service`.

**Стек:** Deno 2.9.x, TypeScript strict, Hono, zod, `node:sqlite`; фронт —
Vite, React 19, TanStack Router / Query / Table / Virtual, zustand, vitest.

**Спека:** `docs/2026-09-17-mpu-objects-design.md` (исполнителю не видна —
рабочие части переносятся в `ts/docs/specs/` порция за порцией).

## Общие ограничения

- Всё — в `mpu/ts/`; подпроекты — папки `ts/<имя>/` со своим `deno.jsonc`,
  объединённые workspace корневого `ts/deno.jsonc`.
- Сеть — только `127.0.0.1`. Порты целевые: `mcp` 7337, `back` 7338. До
  переключения (порция 8) новый `mcp` слушает **7339**, чтобы не мешать
  работающему `mpu-mcp.service` на 7337.
- Новый CLI до переключения ставится как `mpu-next`; `~/.local/bin/mpu` не
  трогается до порции 8.
- Зависимости, согласованные владельцем 2026-09-17: Hono (уже есть),
  `node:sqlite` (встроен), Vite, React 19, TanStack Router / Query / Table /
  Virtual, zustand, vitest. Каждая — строкой «зачем» в `deno.jsonc`. Прочие —
  только с разрешения.
- Go и C — не сейчас; после прототипа и замера (раздел 13 спеки).
- Каждая порция: сценарии «дано → ожидается», спека с разделом «Пункты
  чек-листа», рецензия исполнителя по `review.md` + `review-mpu.md`, отчёт с
  пунктами 4, 5, 8. Постановка несёт ядро и раздел о трате контекста полным
  текстом (`ts/docs/CLAUDE.md`, «Что несёт каждая постановка»).
- systemd, docker и живой MCP-клиент из изолированной сессии недостижимы:
  живые проверки этих порций делает спецификатор при приемке.
- Порядок — переукладка первой (memory: переукладку первым пунктом порции).
- **Рабочий `mpu` не ломается до порции 8.** Порции 2–7 добавляют новые
  модули и новые точки входа (`mpu-next`, `mcp` на 7339); прежние точка входа
  CLI, реестр и `mpu mcp` ведут себя как до порции 1 — их голдены и `smoke`
  зелёные в каждой порции, это пункт приёмки. Установленный
  `~/.local/bin/mpu` пересобирается из дерева в любой момент и должен остаться
  рабочим после любой принятой порции.
- Тесты и smoke не трогают настоящие systemd, `mpu-mcp.service`,
  `~/.local/bin` и порты 7337/7338: запуск процессов и юнитов подменяется,
  порты — выданные ОС (`port: 0`).

---

## Порция 0 (спецификатор): подготовка

Кода нет. Результат:

- [x] **Проба подтверждения MCP.** Маленький тестовый MCP-сервер на петле с
  одним тулом, который просит у клиента yes/no (elicitation). Подключить к
  Claude Code, вызвать тул, записать фактическое поведение: приходит ли вопрос,
  что возвращается на «да», «нет», отказ отвечать. Если elicitation не
  поддержан — то же для MCP Apps. Итог — абзац в разделе 6 спеки архитектуры
  и голден пары «запрос → ответ» в `ts/docs/specs/fixtures/mcp-objects/`.
- [x] **Проверка `node:sqlite` в Deno 2.9.x** на машине оператора: открыть
  файл, записать, прочитать, закрыть; проверить `deno compile` с ним. Не
  работает — выбрать `jsr:@db/sqlite` и записать аргумент.
- [ ] **Глоссарий.** В `ts/docs/glossary.md`: сообщение, приёмник, селектор
  (унарный, ключевой), цепочка, объект «значение», `doesNotUnderstand`,
  правило, решение (`allow` / `ask` / `deny`), наследуемое правило.

Приёмка: три пункта записаны и закоммичены.

Итог 2026-09-17: elicitation поддержан (голден `fixtures/mcp-objects/`, раздел 6
спеки); `node:sqlite` работает в Deno 2.9.6 и в `deno compile` с правами только
`--allow-read`/`--allow-write` на файл базы, без предупреждений.

## Порция 1 (сквозной номер 136, принята 2026-09-17): workspace и переезд `src/` в `ts/back/`

Только переукладка: rename / move, поведение и тесты не меняются.

**Спека:** `ts/docs/specs/platform/workspace.md`.

**Сценарии:**
- дано дерево до порции, `deno task build` → бинарь `mpu`; после порции тот же
  вызов из `ts/` → бинарь, у которого `mpu --help`, `mpu version`, голдены всех
  команд побайтово равны прежним;
- `deno task test` из `ts/` прогоняет тесты всех членов workspace;
- `deno task smoke` зелёный;
- `ts/back/` не импортирует ничего вне себя, кроме `ts/docs/specs/fixtures`
  (закрытый список публикации читается из канала, как сейчас).

**Приёмка спецификатором:** `git log --stat` порции — только перемещения и
правка путей; бинарь собран после последнего коммита (время сборки против
времени коммита); `mpu mcp` из нового бинаря отвечает в Claude Code.

## Порция 2 (сквозной номер 137, принята 2026-09-17): разбор строки в цепочку сообщений

**Спека:** `ts/docs/specs/platform/messages.md` (раздел 2 спеки архитектуры).

**Сценарии (таблица ≥ 15 строк в спеке):**
- `kiten card: 123` → унарное `kiten`, ключевое `{card: "123"}`;
- `sql-ro --seller-id 54 query: "select 1"` → одно ключевое сообщение
  `{seller-id: "54", query: "select 1"}`;
- `seller-id: 5 seller-id: 6` → ошибка «ключ seller-id дважды», код 2;
- `kiten card: 123 comment: "x"`, где метод `card:` не принимает `comment:` →
  два сообщения; то же с `.` между ними → те же два сообщения;
- ключ, который принимают оба звена, без `.` → текущему сообщению;
- `note: -- "a:"` → значение `a:`;
- `card:` в конце строки → ошибка «у ключа card нет значения», код 2;
- пустая строка → сообщение `help` объекту `mpu`;
- `kiten card 123 comment "x"` → четыре унарных звена; что значат `123` и
  `"x"`, решают объекты порции 3, разбор слова не классифицирует;
- неизвестный ключ у последнего звена → `doesNotUnderstand` (порция 3).

**Свойства:** разбор детерминирован; схемы приёмников даются разбору извне
(тест подставляет свои).

## Порция 3a (138) и 3b (139), приняты 2026-09-17: объекты, рефлексия, справка; команды реестра как объекты и точка входа `mpu-next`

**Спека:** `ts/docs/specs/platform/objects.md` (разделы 3–4).

**Сценарии:**
- `mpu selectors` → список селекторов корня, отсортированный;
- `mpu kiten respondsTo: card:` → `true`; `respondsTo: nope:` → `false`;
- `mpu kitn` → `doesNotUnderstand`, в выводе «kiten» как ближайший, код 2;
- `mpu help kiten card` и `mpu kiten card: --help` → побайтово одинаковый текст;
- `mpu help kiten mr` → справка по двум узлам подряд;
- `mpu version name` → `doesNotUnderstand` с текстом «цепочка окончена»;
- `mpu kiten card 123` → объект карточек отвечает на `123` карточкой;
  `mpu kiten card abc` → `doesNotUnderstand` объекта карточек с его текстом;
- `mpu sql-ro 54 "select 1"` → результат запроса; путь для политики —
  `sql-ro <client> <query>`;
- все нынешние команды реестра доступны как сообщения через `mpu-next`:
  прежние строки вызова (`mpu kiten card 123`, `mpu sql-ro 54 "…"`) дают тот же
  результат, что голдены порции 1;
- замена модуля методов в работающем процессе: тест грузит версию A, вызывает,
  подменяет на B, вызывает → ответ B без перезапуска.


## Порция 4 (сквозной номер 140, принята 2026-09-18): политика

**Спека:** `ts/docs/specs/platform/policy.md` (раздел 5).

**Сценарии (таблица специфичности ≥ 12 строк):**
- `kiten card <card> comment → ask`; `mpu kiten card 123 comment "x"` → ask
  для любого номера;
- правила `* → allow`, `sql * → ask`; `mpu sql-ro …` → ask;
  `mpu kiten ls` → allow;
- `kiten * → ask`, `kiten ls → allow`; `mpu kiten ls` → allow;
  `mpu kiten card: 1` → ask;
- цепочка `kiten card: 1 comment: x` при `kiten card: → allow`,
  `kiten card: comment: → ask` → ask;
- любое звено `deny` → отказ, текст называет правило;
- пустая база → посев: нынешние ro → allow, rw → ask; повторный старт посев
  не повторяет;
- `mpu allow: "kiten *"` из tty после «да» → правило записано; следующий
  вызов видит его без перезапуска;
- то же через канал MCP → отказ «политику меняет только человек», база не
  изменилась;
- `mpu policy` → список правил, отсортированный по шаблону;
- база повреждена → старт `back` с ошибкой, называющей файл; молчаливого
  «всё разрешено» нет.

**Инвариант:** решение принимается в одной точке для любого канала.

## Порция 5 (сквозной номер 141, принята 2026-09-18): RPC-сервер `back`

**Спека:** `ts/docs/specs/platform/back-rpc.md` + голдены пар.

**Сценарии:**
- `POST 127.0.0.1:7338/rpc` без токена → 401, тело без токена;
- с токеном, `send` со словами `["kiten","ls"]` и контекстом (cwd, env, tty) →
  результат команды;
- `Origin: http://evil.localhost:7338` → 403; `http://mpu.localhost:7338` → ок;
- `GET /schema` → JSON Schema контракта RPC; голден;
- решение `ask` для канала без tty и без подтверждающей стороны → отказ;
- WebSocket `/stream` → строки лога по мере появления, закрытие клиента
  останавливает источник (санитайзер ресурсов зелёный);
- изменение дерева методов → `~/.cache/mpu/tree.json` перезаписан атомарно;
- `GET /` → статика `web` (пустая заглушка до порции 10).

## Порция 6 (сквозной номер 142, принята 2026-09-18): `cli` — без автоподъёма (он в порции 8), плюс агентский токен

**Спека:** `ts/docs/specs/cli-client.md`.

**Сценарии:**
- `mpu-next kiten ls` при живом `back` → тот же stdout и код, что у `mpu kiten
  ls` порции 3;
- `back` не отвечает, `MPU_NO_AUTOSTART=1` → ошибка «бэкенд не запущен», код
  1, systemctl не вызывался;
- `back` не отвечает, автоподъём разрешён → написан юнит, вызван
  `systemctl --user enable --now mpu`, ожидание готовности, команда
  выполнена (тест подменяет запуск процессов);
- `ask` в tty → вопрос; «n» → отказ, код 1; нет tty → отказ без вопроса;
- время старта `mpu-next help` — замер в отчёте.

## Порция 7a (сквозной номер 143, принята 2026-09-18): строка простым HTTP в `mpu-back`

**Спека:** `ts/docs/specs/platform/back-http-line.md`. `POST /line` и
`/agent/line`: кадры NDJSON потоком или один JSON; подтверждение — вторым
запросом с одноразовым номером. Решение владельца 2026-09-18: свой клиент
(C, `curl`, `http` в nu) должен уметь строку без WebSocket, с потоком длинного
вывода.

## Порция 7 (7b, сквозной номер 144, принята 2026-09-18): `mcp` — на официальном SDK (решение владельца 2026-09-18), к `back` — через HTTP-вход 7a

**Спека:** `ts/docs/specs/platform/mcp-objects.md` + голдены (из порции 0).

**Сценарии:**
- `tools/list` → ровно `help` и `mpu`, схемы — голден;
- `help` с путём `["kiten"]` → то же, что `mpu help kiten`;
- `mpu` при `allow` → результат; при `ask` → запрос подтверждения клиенту
  (форма из порции 0); «нет» → отказ; при `deny` → отказ;
- `mpu` со словами `["allow:", "*"]` → отказ без обращения к клиенту;
- `back` перезапускается во время вызова → повтор до таймаута, затем
  ошибка с именем `back`;
- запрос с неизвестным `mcp-session-id` → 404 (не 400);
- новый метод в `back` → виден в `help` без перезапуска `mcp`.

**Приёмка спецификатором:** живой прогон в Claude Code на порту 7339.

## Порция 8: `supervisor`, `install.sh`, переключение

**Спека:** `ts/docs/specs/platform/supervisor.md`, `ts/docs/specs/install.md`
(раздел 11).

**Сценарии:**
- супервизор запускает `back` и `mcp`; `back` упал → перезапущен только он;
  `mcp` тот же PID;
- команда супервизору «перезапусти back» → то же;
- SIGTERM супервизору → оба дочерних остановлены, код 0;
- `install.sh` без изменений → ничего не перезапущено;
- `install.sh --only back` → пересобран и перезапущен только `back`;
- провал проверки → ненулевой код и имя шага;
- переключение: `mpu-next` → `mpu`, `mcp` → 7337, `mpu-mcp.service`
  выключен после успешной проверки; откат — одной командой, описанной в спеке.

**Приёмка спецификатором:** живой прогон на машине оператора; замер памяти
всех процессов (заполнить таблицу раздела 13 спеки).

## Порция 9: `complete`

**Спека:** `ts/docs/specs/complete.md` (раздел 9). Нынешний режим дополнения
(`src/entrypoint/completion.ts`) — исходная точка, его голдены остаются.

**Сценарии:**
- `mpu ki<TAB>` → `kiten`; `mpu kiten card: <TAB>` → значения от `back`;
- `back` остановлен → селекторы из `tree.json`, значений нет, за ≤ 150 мс;
- `seller-id:` в bash не разрывается;
- `mpu-complete init nu` → голден скрипта;
- дополнение не пишет в журнал вызовов и не поднимает юнит.

## Порция 10: `web`

**Спека:** `ts/docs/specs/web-policy.md` (раздел 10).

**Сценарии:**
- `http://mpu.localhost:7338` → дерево объектов из рефлексии;
- смена узла `kiten` на `ask` → следующий `mpu kiten ls` спрашивает;
- у родителя с разными детьми — частичное состояние;
- «сбросить» → узел снова наследует;
- `back` недоступен → сообщение на странице, а не пустое дерево.

Сборка — `deno run -A npm:vite build`; тесты — vitest.

---

## Покрытие спеки

| Раздел спеки | Порция |
|---|---|
| 1 Подпроекты | 1, 8 |
| 2 Грамматика | 2 |
| 3 Объекты | 3 |
| 4 Справка | 3 |
| 5 Политика | 4 |
| 6 Подтверждение | 0, 6, 7 |
| 7 MCP | 7 |
| 8 CLI | 6 |
| 9 Дополнение | 9 |
| 10 Фронт | 10 |
| 11 Установка | 8 |
| 12 Порядок | весь план |
| 13 Ресурсы | 8 (замер) |
