# План переезда — команды по частоте использования

Приоритетный список для фазы 2 мастер-плана
(`2026-08-04-full-migration.md`); там же — фазы, критерии выхода и цикл
переезда одной команды. Единица переезда — команда; спеки живут в канале `ts/docs/specs/`
(правила — `ts/docs/CLAUDE.md`), статусы («—» / черновик / к реализации /
реализовано) — в самих спеках, здесь — зеркало для планирования.
Источник частот: журнал вызовов mpu, 21–27.07.2026 (вся доступная
история — окно короче 6 месяцев), 3197 вызовов. Перед стартом переезда
команд частоты пересобрать по свежему журналу — это уточнит порядок;
состав «Не едут» (решён 2026-07-27) пересмотру не подлежит. Порядок
списка — приоритет ценности; реализация идёт сверху вниз.

История: план заведён для отменённой Go-редакции (брокер + CLI);
2026-08-04 адаптирован под текущую архитектуру — один Deno/TS-бинарь,
CLI + MCP-сервер, не переехавшие команды исполняются маршрутом `legacy`
(подпроцесс Python-реализации). Спеки перенесены из `go/docs` в
`ts/docs/specs/` и раз-брокерены.

## Обвязка — реализована (MVP 2026-08-03)

- **mcp-server** — агентная точка входа: Streamable HTTP, профили
  `ro`/`rw`, закрытый список публикации, аннотации необратимых тулов.
  Заменил протокол брокера (`protocol/core`/`protocol/legacy` умерли).
  Спека: `ts/docs/specs/platform/mcp-server.md` — реализовано.
- **registry** — реестр команд: маршрутизация `native`/`legacy`,
  справка, completion, мост в Python. Поглотил брокерную
  legacy-делегацию. Спека: `ts/docs/specs/platform/registry.md` —
  реализовано.
- **command-contract** — контракт объявления команды (`path`, `policy`,
  схемы, `render`). Спека: `ts/docs/specs/platform/command-contract.md`.
- **config** — локальные предпочтения + `mpu config`. Спека:
  `ts/docs/specs/platform/config.md` — черновик.
- **env-file** — слой секретов/подключений native-команд (бывший конфиг
  брокера). Спека: реализовано (2026-08-04).
- **selector** — резолв `sl-N`/имени; исполняется самим CLI. Спека:
  черновик (адаптирована 2026-08-04).
- **invoke-log** — локальный журнал вызовов, формат оригинала. Спека:
  черновик (адаптирована 2026-08-04).
- **readonly-default** — чтение по умолчанию: read-only PG-сессия +
  разделение MCP-профилей. Спека: черновик (адаптирована 2026-08-04).

## Команды к переезду (по частоте)

1. **sql-ro** (1225) — SQL в enforced read-only сессии на PG по
   селектору. Атомы: selector, env-file, readonly-default. Спека:
   черновик.
2. **logs** (392) — логи сервисов стенда (Loki; legacy — Portainer).
   Спека: черновик.
3. **kiten** (390) — Kaiten из терминала. Дробится по подкомандам:
   time (153) — учёт времени и таймер; card (82) — чтение карточки;
   field (43) — кастомные поля, в т.ч. AI-артефакт; status (37) — вся
   моя работа матрицей; close (25); comment (19); ls (10); roles (8);
   ready (2); справочники. Атом: platform/kaiten-http. Спеки: черновик
   (card, ls, status, refs, time, move, close, comment, field + атом).
4. **ssh** (177) — команда в `mp-sl-N-cli` или произвольном контейнере.
   Атом: platform/exec-transport. Спека: черновик.
5. **api** (150) — HTTP-клиенты sl-back. Дробится по эндпоинтам:
   wb-loader-blocked (59), wb-loader-status (52), wb-loader-resume (4),
   остальные по мере надобности. Атом: platform/slback-http
   (авторизация, базовые URL). Спеки: черновик (api, api-wb-loader,
   platform/slback-http).
6. **sheet** (119) — Google Sheets через Apps Script webapp. Дробится:
   get (78), ls (20), batch-update (8), batch-get. Атомы:
   platform/webapp-http, кэш листов. Спеки: черновик (sheet,
   sheet-batch + атом).
7. **mr** (103) — GitLab MR review. Дробится: view (25), comment (17),
   comments (15), files (7), create (7), describe (6), reply (5),
   note (5), diff (3), resolve (2). Атом: platform/gitlab-api
   (positioned-комментарии — самая хрупкая часть, отдельная спека).
   Спеки: черновик (mr-read, mr-write + атом gitlab-api).
8. **run-js** (61) — ESM-код внутри контейнера sl-back. Атомы общие
   с `ssh` (exec-transport). Спека: черновик.
9. **ps** (47) — список контейнеров. Атом: кэш контейнеров. Спека:
   черновик.
10. **telegram** (40) — от имени пользователя: status (16), search (10),
    send (6), ls (5). MTProto — может остаться на Python неограниченно;
    решать при её очереди, не раньше. Спека: черновик.
11. **glab-status** (36) — таблица прохождения MR по веткам
    деплой-пайплайна. Атом общий с `mr` (gitlab-api). Спека: черновик.
12. **xlsx** (27) — локальные .xlsx: get (18), ls (5). Первый
    вертикальный срез — реализовано (native, обе точки входа). Спека:
    `ts/docs/specs/xlsx.md`.
13. **search** (25) — клиент по email/ID → client_id (локальный кэш;
    impersonation 10X). Инвариант: вывод не пишется в журнал (токены).
    Спека: черновик.
14. **log** (15) — просмотр журнала вызовов (запись — в обвязке).
    Спека — в `platform/invoke-log.md` (черновик).
15. **d2-miro** (7) — рендер D2-диаграммы в Miro. Спека: черновик.
16. **copy-shared** (6) — копия shared-таблиц с сервера по селектору.
    Спека: черновик.
17. **ss-update** (5) — Portainer-обёртка пересчёта таблиц. Атом:
    platform/portainer — общий для всего семейства обёрток (п. 20).
    Спека — в `portainer-wrappers.md` (черновик).
18. **health** (5) — статусы контейнеров + tail логов виновников.
    Спека: черновик.
19. **sql** (4) — write-capable SQL: мутации с подтверждением. Спека —
    поверх sql-ro (общая обвязка PG, разница в режиме и подтверждении).
    Спека: черновик.
20. **Семейство Portainer-обёрток над `node cli service:*`** —
    wb-loader (3), ozon-recalculate-expenses (2), data-loader (1),
    wb-loader-load (1), wb-recalculate-expenses (1),
    wb-save-expenses (1), ozon-save-expenses (1). Один общий атом
    (platform/portainer + шаблон обёртки) и тонкие спеки-строчки.
    Спеки: черновик (portainer-wrappers + атом portainer).
21. **Локальный стенд и переносы клиентов** — copy-client (4),
    copy-dev (2), make-schema (1), mp-init (1), move-client (1),
    move-client-back (1), clean-local-clients (1). Редкие, но
    многоразовые; едут в хвосте. Спеки: черновик (7 файлов,
    move-client-back — отдельной спекой).
22. **init** (2) / **update** (1) — инфраструктура кэша: bootstrap
    служебной БД + discovery контейнеров; полный синк снапшота кэша с
    PG (+ точечный по одному клиенту). Частота нерепрезентативна —
    вызываются редко и автоматически, но без них селектор
    нежизнеспособен на чистой машине; едут перед первой командой,
    зависящей от кэш-БД. Спеки: init — к реализации (2026-08-04,
    переработана: конкурентность + инварианты устойчивости; порции
    А/Б — `~/tmp/task-init-a-prompt.txt` / `-b-`), update — черновик.

## Не едут — решено 2026-07-27

- **ozon-fix-fo-tax** (2) — data-fix конкретного инцидента.
- **backup-wb-unit-proto / backup-ozon-unit-proto /
  backup-wb-unit-manual-data** (4/1/1) — CTAS-бэкапы под ремедиации.
- **wb-unit-proto-new** (1) — разовая миграция таблицы.
- **sun** (1) — восход/закат; вне домена.
- **0 вызовов за окно**: process, ss-load, ss-datasets, wb-unit-calc,
  wb-jobs, iu-wb, ozon-loader, ozon-jobs, data-loader-jobs, users,
  clients-migrations, datasets-migrations, app-migrations, confirm.
- CLI-инфраструктура и шум журнала (`--help`, `version`, опечатки) — не
  команды, поглощаются обвязкой.

Причины смерти — в `docs/migration-log.md`.
