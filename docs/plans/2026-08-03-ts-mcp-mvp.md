# MVP: MCP-сервер и контракт команды на Deno/TS — план реализации

> **Для исполнителей:** план ведётся сессией-спецификатора **вне канала спек**
> (`ts/docs/CLAUDE.md`, строка 42). Задачи двух видов, они не смешиваются:
> **[С]** — сессия-спецификатор, пишет только в `ts/docs/`;
> **[D]** — изолированная Deno-сессия, пишет только код внутри `ts/`;
> **[O]** — оператор вне репозитория (конфиг Claude Code, запуск).
> Шаги отмечаются чекбоксами (`- [ ]`).

**Goal:** доказать конструкцию «один бинарь = CLI для человека + MCP-сервер для агента» насквозь на одной уже написанной вертикали (`xlsx`), ничего не добавляя к функционалу.

**Architecture:** команда объявляет схему аргументов, схему результата, политику `ro|rw` и рендер; `run` возвращает данные и ничего не печатает. Режим CLI рендерит их в текст, режим MCP отдаёт как `structuredContent`. Транспорт MCP — Streamable HTTP на `127.0.0.1`, роутер Hono, два непересекающихся профиля `/ro` и `/rw`. Непереехавшие команды исполняются подпроцессом Python-`mpu` по маршруту `legacy` и тулами не становятся.

**Tech Stack:** Deno 2.x, TypeScript strict, `jsr:@zod/zod`, `jsr:@hono/hono`, `@std/assert`.

**Источник истины по архитектуре:** `mpu/docs/ts-mcp-architecture.md`. Этот план не повторяет её обоснований и не спорит с ней; расхождение = дефект плана.

## Global Constraints

Действуют в каждой задаче, повторно в задачах не перечисляются.

- **Протокол MCP — ревизия `2026-07-28`.** Stateless: нет `initialize`, нет `Mcp-Session-Id`, сервер не инициирует JSON-RPC-запросы.
- **Зависимости только с JSR, версии пинятся точно, не через `^`.** Deno блокирует пакеты моложе 24 часов (minimum dependency age): `jsr:@zod/zod@4.4.3`, `jsr:@hono/hono@4.12.33`. Каждая запись в `deno.jsonc` сопровождается комментарием «зачем». `npm:`-зависимостей нет.
- **Сервер слушает `127.0.0.1`, порт `7337`.** Не `0.0.0.0`.
- **Имена серверов в `.mcp.json` — `mpu_ro` и `mpu_rw`** (через `_`, не через дефис). Endpoint'ы — `/ro` и `/rw` одного процесса.
- **Имя тула = путь команды через `_`**, сегменты kebab тоже через `_`: `mpu kiten field artefact set` → `kiten_field_artefact_set`. Префикс `mpu_` не добавляется — имя сервера уже его несёт.
- **`z.toJSONSchema` вызывается с `{ io: "input" }` для схемы аргументов и `{ io: "output" }` для схемы результата.** Без этого поля с `default` попадают в `required`.
- **Гейты перед объявлением задачи готовой:** `deno fmt --check && deno lint && deno check . && deno test`. Пока не зелёные — работа не выполнена и об этом не сообщается.
- **Git:** только ветка `main`, веток не создавать. Коммит с pathspec: `git commit -m "…" -- ts/`. `git add -A`, `git commit -a`, `git stash` запрещены (блокируются хуком).
- **Сообщения коммитов:** Conventional Commits со скоупом, тема на русском в императиве (`feat(mcp): добавить …`).
- **Ошибки:** exit 2 — ошибки ввода, exit 1 — инфраструктура; stderr без трейсбеков.
- **Язык:** комментарии и документация — русские, идентификаторы — английские.

## Ограничение формата

Skill `writing-plans` требует показывать в шагах готовый код. Здесь это **сознательно не соблюдается для задач [D]**: `ts/docs/CLAUDE.md` устанавливает, что типы и модули проектирует изолированная Deno-сессия, а спека фиксирует контракт возможности, а не API. Поэтому задачи [D] задают **вход, наблюдаемый результат, инварианты и команды проверки**, но не сигнатуры. Задачи [С] показывают содержание спеки полностью — там плейсхолдеров нет.

## Порядок задач [D]

**Task 10 выполняется первой среди задач [D], до Task 6.** `ts/CLAUDE.md` — главный и почти единственный контекст изолированной сессии; пока он описывает брокерную архитектуру, сессия рассуждает о брокере даже там, где спеки его не упоминают. Проверено на практике: первая же сессия обосновала зависимость «разбором ответов брокера (JSON-RPC)».

Остальные [D] идут по номерам: 6 → 7 → 8 → 9.

---

## Task 1 [С]: Вычистить канал от брокера

Канал описывает несуществующую систему: `glossary.md` — четыре термина из шести про брокер, `config.md` — ключ `broker.url` и тезис «секреты живут у брокера», `registry.md` — маршрут «операции брокера».

**Files:**
- Modify: `ts/docs/glossary.md`
- Modify: `ts/docs/specs/platform/config.md`
- Delete: `ts/docs/specs/protocol` (симлинк), `ts/docs/specs/fixtures/protocol` (симлинк)

- [ ] **Шаг 1: переписать `glossary.md`**

Удалить строки `брокер`, `операция`, `legacy-делегация`, `токен`. Итоговая таблица:

| Термин | Имя в коде | Пояснение |
| --- | --- | --- |
| реестр команд | `registry` | единый список команд: маршрутизация + справка |
| маршрут | `route` | способ исполнения команды: `native` (код на TS) или `legacy` (подпроцесс Python-реализации) |
| политика | `policy` | объявленный класс команды: `ro` — только читает, `rw` — мутирует |
| профиль | `profile` | набор тулов одной политики, публикуемый MCP-сервером (`ro` или `rw`) |
| тул | `tool` | команда маршрута `native`, опубликованная в MCP |
| схема аргументов | `argsSchema` | описание входа команды; из него строятся и разбор argv, и `inputSchema` тула |
| схема результата | `resultSchema` | описание выхода команды; из него строится `outputSchema` тула |
| рендер | `render` | преобразование результата команды в текст для человека |
| токен доступа | `accessToken` | статический Bearer для MCP-сервера; файл 0600 |

- [ ] **Шаг 2: почистить `config.md`**

В «CLI-контракт» из закрытого реестра ключей убрать `broker.url`, добавить `mcp.port` (int, дефолт `7337`) и `mcp.legacy_bin` (str, дефолт `~/.local/share/uv/tools/mpu/bin/mpu`). В «Назначение» и «Инварианты» заменить «токен брокера» на «токен доступа MCP-сервера (файл 0600, контракт — `specs/platform/mcp-server.md`)». В «Известных отклонениях» переписать пункт про упразднённый env-файл, убрав «секреты — только у брокера». Статус спеки остаётся «черновик».

- [ ] **Шаг 3: удалить симлинки канала протокола**

```bash
rm ts/docs/specs/protocol ts/docs/specs/fixtures/protocol
```

- [ ] **Шаг 4: убрать упоминания протокола из `ts/docs/CLAUDE.md`**

Удалить пункт `specs/protocol/` из «Структуры»; в «Правилах для изолированной Deno-сессии» удалить пункт про словарь операций брокера; в «Чек-листе спецификатора» из строки про самодостаточность убрать `specs/protocol/`.

- [ ] **Шаг 5: проверка**

```bash
grep -rn "брокер\|broker\|JWT\|specs/protocol" ts/docs/ | grep -v "registry.md"
```
Ожидание: пусто. `registry.md` исключён намеренно — там 8 упоминаний, и они снимаются вместе с переделкой модели маршрутов в Task 3, а не механической заменой слов.

Отдельно проверить, что термин не просочился в исторические формулировки («после отмены брокера», «ушло к брокеру»): канал самодостаточен, а понятия «брокер» в `glossary.md` больше нет — изолированная сессия его не поймёт. Факт изменения оставлять, понятие — убирать.

- [ ] **Шаг 6: коммит**

```bash
git add ts/docs && git commit -m "docs(specs): вычистить канал от отменённого брокера" -- ts/docs
```

---

## Task 2 [С]: Спека `platform/command-contract.md`

Контракт возможности «команда», из которого обе точки входа получают всё нужное. Формулировки — наблюдаемые и проверяемые; типов и модулей в спеке нет.

**Files:**
- Create: `ts/docs/specs/platform/command-contract.md`

- [ ] **Шаг 1: написать спеку по скелету канала**

Разделы в порядке скелета (`Назначение / CLI-контракт / Ввод-вывод / Побочные эффекты / Конфигурация / Инварианты / Граничные случаи и ошибки / Golden-примеры / Известные отклонения / Открытые вопросы`). Обязательно зафиксировать:

*Контракт.* Каждая команда маршрута `native` объявляет: путь (сегменты после `mpu`); однострочное назначение; подробную справку; политику `ro|rw`; схему аргументов; схему результата; правило рендера результата в текст.

*Инварианты (каждый — проверяемый тестом).*
1. Исполнение команды ничего не печатает: весь вывод порождается рендером из результата. Проверка: прогон команды с подставленным приёмником вывода не даёт записей до вызова рендера.
2. Рендер — функция результата и аргументов, без обращений к сети, диску и часам. Проверка: рендер вызывается на литеральном результате без окружения.
3. Политика объявлена, а не выводится из значений аргументов. Проверка: у каждой команды реестра политика присутствует; команды, у которой мутирующесть зависела от флага, в реестре нет — вместо неё две команды с разными путями.
4. Множество имён параметров в схеме аргументов совпадает с множеством имён, принимаемых из argv. Проверка: обход реестра, сравнение множеств.
5. Обязательность параметра одинакова в обеих точках входа: параметр со значением по умолчанию не обязателен ни в argv, ни в схеме.
6. Результат сериализуем без потерь: рендер в JSON доступен для любой команды и не является её собственным флагом.

*Ошибки.* Ошибка ввода и доменная ошибка различимы по типу, а не по тексту. Отображение в exit-коды — `registry.md`; отображение в MCP — `mcp-server.md`.

*Известные отклонения.* **fix** — флаги формата вывода (`--json`, `--tsv`, `--raw`, `--md`) перестают быть параметрами команд: форма вывода — свойство точки входа. **fix** — флаг, меняющий политику команды (`--print`), упраздняется: read-вариант становится отдельной командой со своим путём.

- [ ] **Шаг 2: проверка по чек-листу спецификатора**

Пройти чек-лист из `ts/docs/CLAUDE.md`: каждое утверждение наблюдаемо и проверяемо; секретов нет; ссылки только внутрь `ts/docs/`; объём ≤ 200 строк; термины сверены с `glossary.md`. Поставить статус «к реализации».

- [ ] **Шаг 3: коммит**

```bash
git add ts/docs && git commit -m "docs(specs): контракт команды — политика, схемы, рендер" -- ts/docs
```

---

## Task 3 [С]: Правка `platform/registry.md` под два маршрута

**Files:**
- Modify: `ts/docs/specs/platform/registry.md`

- [ ] **Шаг 1: заменить модель маршрутов**

Везде, где перечислены маршруты «локальная | операции брокера | legacy-делегация», оставить два: `native` (исполняется кодом TS) и `legacy` (исполняется подпроцессом Python-реализации). Убрать «Исполнение не переехавших команд — операция делегации брокера».

- [ ] **Шаг 2: дописать контракт маршрута `legacy`**

Наблюдаемое поведение legacy-команды не отличается от прямого вызова Python-реализации: stdout, stderr и exit-код прокидываются насквозь, `--help` рендерится ею же. Путь к бинарю берётся из ключа конфига `mcp.legacy_bin`. Отсутствие бинаря по пути → exit 1, stderr `mpu: legacy-реализация не найдена по пути "<путь>"`.

- [ ] **Шаг 3: дописать инвариант видимости**

`mpu --help` и `mpu help` перечисляют команды обоих маршрутов. Команда маршрута `legacy` не публикуется как тул ни в одном профиле. Проверка: пересечение множества имён `legacy`-команд и множества имён тулов пусто.

- [ ] **Шаг 4: коммит**

```bash
git add ts/docs && git commit -m "docs(specs): реестр — маршруты native и legacy вместо брокера" -- ts/docs
```

---

## Task 4 [С]: Спека `platform/mcp-server.md`

**Files:**
- Create: `ts/docs/specs/platform/mcp-server.md`
- Create: `ts/docs/specs/fixtures/mcp-server/*.json` (golden-пары запрос/ответ)

- [ ] **Шаг 1: написать спеку**

*Контракт.* `mpu mcp [--profile ro|rw|ro,rw] [--port N]` — долгоживущий процесс, Streamable HTTP. Слушает `127.0.0.1`, порт из `--port`, иначе из ключа конфига `mcp.port`, иначе `7337`. Профиль `ro` обслуживается на пути `/ro`, `rw` — на `/rw`.

*Обязательное поведение транспорта (ревизия 2026-07-28).*
- Только `POST`. `GET`, `DELETE` и прочее → `405`.
- Заголовок `Origin` присутствует и не входит в разрешённый список → `403`.
- Отсутствует `Authorization: Bearer <токен доступа>` или токен неверен → `401`.
- На каждом POST обязательны `MCP-Protocol-Version` и `Mcp-Method`; для `tools/call` — ещё `Mcp-Name`. Отсутствие любого из них или расхождение с телом → `400` + JSON-RPC `-32020`. Значения в сентинеле `=?base64?…?=` декодируются перед сравнением.
- Версия протокола вне поддерживаемых → `400` + `UnsupportedProtocolVersionError` со списком поддерживаемых.
- Неизвестный метод → `404` + JSON-RPC `-32601`.
- Тело — JSON-RPC нотификация → `202` без тела.
- Ответ на запрос — всегда `Content-Type: application/json`. SSE в этой редакции не реализуется (спека требует «либо JSON, либо SSE»).
- `Mcp-Session-Id` и `Last-Event-ID` игнорируются.

*Методы.* `server/discover` — версии протокола, capabilities, идентичность и **версия сборки бинаря**. `tools/list` — тулы профиля этого пути. `tools/call` — исполнение.

*Отображение результатов.* Успех → `structuredContent` по схеме результата команды. Доменная ошибка → результат с `isError: true` и человекочитаемым текстом, **не** JSON-RPC error. Ошибка ввода (аргументы не прошли схему) и неизвестный тул → JSON-RPC error.

*Аннотации тулов.* `readOnlyHint: true` для профиля `ro`. Для команд с необратимым внешним эффектом — `_meta` с флагом обязательного подтверждения на каждый вызов.

*Инструкции сервера.* Каждый профиль объявляет текст инструкций, не длиннее **2048 байт** (клиент режет ровно на этом): он не перечисляет команды, а объясняет, для каких задач искать тулы этого сервера. Тот же предел — на описание одного тула. Проверка: длина обоих текстов измеряется тестом.

*Объём ответа.* Результат тула, чей размер не ограничен по построению (выборка строк, чтение диапазона, хвост лога), обязан иметь в схеме аргументов ограничитель со значением по умолчанию; в результате — признак усечения. Команда, у которой большой ответ осмыслен целиком, объявляет повышенный предел размера результата через `_meta`. Проверка: у каждого тула, помеченного как потенциально объёмный, ограничитель присутствует и имеет дефолт.

*Инварианты.*
1. Профили не пересекаются: множество имён тулов `/ro` и `/rw` не имеет общих элементов.
2. На `/ro` нет ни одного тула с политикой `rw`.
3. Имя тула однозначно восстанавливает путь команды и уникально в пределах профиля.
4. `tools/list` не содержит команд маршрута `legacy`.
5. Ни один ответ не содержит секретов: токен доступа не печатается и не возвращается.

*Конфигурация.* Токен доступа — файл 0600 в конфиг-каталоге, генерируется при первом запуске сервера. Подкоманда, печатающая заголовок авторизации для `headersHelper`, — часть контракта.

*Golden-примеры.* Минимум по одному на класс: успешный `tools/list`; успешный `tools/call`; `tools/call` с невалидными аргументами; `tools/call`, где команда упала доменной ошибкой; `400` при расхождении `Mcp-Name` с телом; `405` на GET.

- [ ] **Шаг 2: снять golden-пары**

Фикстуры синтетические (живых данных в канале быть не может): пары «запрос → ответ» для перечисленных классов, по файлу на класс, в `ts/docs/specs/fixtures/mcp-server/`.

- [ ] **Шаг 3: чек-лист и статус «к реализации»**

- [ ] **Шаг 4: коммит**

```bash
git add ts/docs && git commit -m "docs(specs): MCP-сервер — Streamable HTTP, профили, отображение ошибок" -- ts/docs
```

---

## Task 5 [С]: Дописать `xlsx.md` структурной формой результата

`xlsx` уже реализован и его CLI-поверхность заморожена golden'ами. Меняется только то, что результат становится наблюдаемым структурно.

**Files:**
- Modify: `ts/docs/specs/xlsx.md`

- [ ] **Шаг 1: зафиксировать структурный результат каждой подкоманды**

Для `get`, `ls`, `resolve`, `alias ls` описать форму результата — она совпадает с существующим JSON-выводом, зафиксированным golden-фикстурами `get-range.json`, `ls-json.txt`. Для `alias add`, `alias rm`, `open` описать форму результата действия (что именно изменилось / что было запущено).

- [ ] **Шаг 2: проставить политику подкомандам**

`get`, `ls`, `resolve`, `alias ls` — `ro`. `alias add`, `alias rm` — `rw` (пишут локальное хранилище). `open` — `rw` (запускает внешний процесс).

- [ ] **Шаг 3: отметить упразднение флагов формата**

В «Известных отклонениях» — **fix**: `--json`, `--tsv`, `--raw` перестают быть флагами подкоманды `get`; выбор формы вывода переезжает на уровень CLI. Текстовые формы `tsv` и `raw` сохраняются как режимы рендера и остаются покрыты прежними golden-фикстурами.

- [ ] **Шаг 3б: обернуть массив в объект у `ls`**

Структурное содержимое ответа тула передаётся объектом, поэтому результат команды обязан быть объектом в корне (инвариант 7 `command-contract.md`). `mpu xlsx ls` возвращает массив — он оборачивается полем `sheets`. Записать в «Известные отклонения» `xlsx.md` вердиктом **fix** и обновить golden-эталон `ls-json.txt`: это второе и последнее санкционированное изменение фикстур.

- [ ] **Шаг 3а: обезвредить снапшот-путь в фикстурах**

Четыре фикстуры канала — `get-range.json`, `get-empty-sheet.json`, `get-formula-error-merge.json`, `err-not-zip.txt` — содержат абсолютный путь конкретной машины внутрь каталога `go/`, который миграция удаляет. Заменить его нейтральным плейсхолдером и зафиксировать плейсхолдер в спеке `xlsx.md`: подстановка реального каталога — обязанность теста, а не свойство фикстуры.

Тесты читают копии из тестового каталога модуля, а не канал, поэтому правка фикстур канала ничего не ломает; парная правка кода — Task 6, шаг 4.

- [ ] **Шаг 4: коммит**

```bash
git add ts/docs && git commit -m "docs(specs): xlsx — структурный результат и политика подкоманд" -- ts/docs
```

---

## Task 6 [D]: Контракт команды и перебор `xlsx`

Первая изолированная сессия. Вход — `specs/platform/command-contract.md`, `specs/xlsx.md`, `specs/platform/registry.md`.

**Files:** код внутри `ts/src/` — раскладку проектирует сессия.

- [ ] **Шаг 1: добавить зависимость**

`jsr:@zod/zod@4.4.3` в `deno.jsonc` с комментарием «зачем»; сгенерировать и закоммитить `deno.lock`.

- [ ] **Шаг 2: тесты инвариантов контракта — красные**

Написать тесты на шесть инвариантов из `command-contract.md`, прогнать, убедиться что падают по ожидаемой причине.

- [ ] **Шаг 3: реализовать контракт и реестр, довести до зелёного**

- [ ] **Шаг 4: переложить пять подкоманд `xlsx` на контракт**

Критерий неизменности поведения: все 12 существующих golden-фикстур `xlsx` проходят **байт-в-байт** (после подстановки снапшот-плейсхолдера — она часть теста, а не результата). Ни одна фикстура не правится по содержанию: правка означает регресс, а не адаптацию.

Санкционированных изменений фикстур ровно два, оба из Task 5, и оба обязаны быть уже внесены в канал до начала этой задачи:

- шаг 3а — плейсхолдер пути вместо абсолютного каталога внутри `go/`: перекопировать четыре обновлённые фикстуры из канала в тестовый каталог модуля и заменить захардкоженную константу снапшот-каталога на плейсхолдер. Проверка после — `grep -rn "mpu/go/" ts/`, ожидание: пусто;
- шаг 3б — `ls-json.txt` становится объектом `{"sheets": […]}` вместо массива.

Любое другое расхождение с фикстурой — регресс, а не адаптация.

- [ ] **Шаг 5: гейты и отчёт**

```bash
deno fmt --check && deno lint && deno check . && deno test --coverage=cov && deno coverage cov
```

Отчёт по форме `ts/CLAUDE.md`: файлы и смысл изменений; покрытие затронутых модулей; новые доменные имена для внесения в глоссарий; замечания к соседнему коду отдельным списком.

- [ ] **Шаг 6: коммит**

```bash
git commit -m "feat(command): контракт команды и перевод xlsx на него" -- ts/
```

---

## Task 7 [D]: Ядро диспетчера

Вход — `specs/platform/mcp-server.md`, разделы «Методы», «Отображение результатов», «Инварианты».

- [ ] **Шаг 1: тесты — красные**

Таблица «JSON-RPC запрос → ответ» по golden-парам из `specs/fixtures/mcp-server/`, скопированным в тестовый каталог модуля. Сети в тестах нет.

- [ ] **Шаг 2: реализовать `server/discover`, `tools/list`, `tools/call`**

- [ ] **Шаг 3: тесты инвариантов профилей**

Непересечение профилей; отсутствие `rw` на `ro`; отсутствие `legacy` в `tools/list`; уникальность имён; длина инструкций профиля и описания каждого тула ≤ 2048 байт. Плюс snapshot `tools/list` по каждому профилю — он ловит переименования и изменения схем одним падением.

- [ ] **Шаг 3а: тест отсутствия корневого комбинатора**

Ни одна схема аргументов не содержит `anyOf`, `oneOf` или `allOf` на верхнем уровне: клиент их там не принимает и сплющивает схему, теряя проверку обязательности. Проверка: обход `tools/list`, отсутствие этих ключей в корне каждого `inputSchema`.

- [ ] **Шаг 4: гейты, отчёт, коммит**

```bash
git commit -m "feat(mcp): ядро диспетчера — discover, tools/list, tools/call" -- ts/
```

---

## Task 8 [D]: HTTP-адаптер на Hono

Вход — `specs/platform/mcp-server.md`, раздел «Обязательное поведение транспорта».

- [ ] **Шаг 1: добавить зависимость**

`jsr:@hono/hono@4.12.33` в `deno.jsonc` с комментарием «зачем»; обновить `deno.lock`. Версия пинится точно — `^` ломает сборку в день релиза апстрима.

- [ ] **Шаг 2: тесты транспорта — красные**

По одному на каждое требование: `405` на GET и DELETE; `403` на чужой `Origin`; `401` без токена и с неверным; `400` + `-32020` при отсутствии каждого из трёх обязательных заголовков и при расхождении каждого с телом, включая случай с сентинелом `=?base64?…?=`; `400` + `UnsupportedProtocolVersionError`; `404` + `-32601`; `202` на нотификацию; `Content-Type: application/json` на успехе. Тесты поднимают сервер на порту `0` и ходят в него `fetch`.

- [ ] **Шаг 3: реализовать адаптер и токен доступа**

Роутинг `/ro` и `/rw` в один диспетчер; bind `127.0.0.1`; генерация токена в файл 0600 при первом запуске; подкоманда печати заголовка авторизации.

- [ ] **Шаг 4: гейты, отчёт, коммит**

```bash
git commit -m "feat(mcp): HTTP-транспорт на Hono с двумя профилями" -- ts/
```

---

## Task 9 [D]: Маршрут `legacy` на пилотной команде

Вход — `specs/platform/registry.md`, раздел про `legacy`.

- [ ] **Шаг 1: тесты — красные**

Подстановка фейкового исполнителя подпроцесса: stdout, stderr и exit-код прокидываются насквозь; отсутствующий бинарь даёт exit 1 и текст из спеки. Реального Python в тестах нет.

- [ ] **Шаг 2: реализовать маршрут и внести `sql-ro` в реестр как `legacy`**

- [ ] **Шаг 3: тест видимости**

`mpu help` содержит `sql-ro`; `tools/list` обоих профилей — не содержит.

- [ ] **Шаг 4: гейты, отчёт, коммит**

```bash
git commit -m "feat(registry): маршрут legacy и пилотная команда sql-ro" -- ts/
```

---

## Task 10 [D]: Привести `ts/CLAUDE.md` в соответствие — ПЕРВАЯ среди [D]

Корневой CLAUDE.md модуля правит только изолированная сессия — сессия-спецификатора туда писать не может.

- [ ] **Шаг 1: переписать раздел «Замысел»**

Вместо «CLI и брокер» — «CLI и MCP-сервер»: один бинарь, два режима, демон только на стороне агента.

- [ ] **Шаг 2: переписать «Песочница и permissions»**

Зафиксировать честно: недоверенной стороны больше нет, права Deno — гигиена, а не барьер; enforcement живёт в read-only-сессии PG и в правилах разрешений клиента.

- [ ] **Шаг 3: переписать «Секреты»**

Вместо временного JWT брокера — статический токен доступа MCP-сервера, файл 0600, не логируется и не попадает в сообщения об ошибках. Удалить абзац «Операции брокера — только из словаря `docs/specs/protocol/`».

- [ ] **Шаг 4: обновить задачу `build` в `deno.jsonc`**

Права под новую модель; каждое расширение — отдельной строкой с обоснованием.

- [ ] **Шаг 5: коммит**

```bash
git commit -m "docs(ts): привести CLAUDE.md модуля к архитектуре без брокера" -- ts/
```

---

## Task 11 [O]: Подключение и приёмка

- [ ] **Шаг 1: собрать и установить бинарь**

```bash
cd ts && deno task build
```

Проверить, что `~/.local/bin/mpu` — новый бинарь, и запомнить: `uv tool upgrade mpu` его затирает.

- [ ] **Шаг 2: поднять сервер**

```bash
mpu mcp --profile ro,rw --port 7337
```

- [ ] **Шаг 3: прописать серверы в `.mcp.json`**

```json
{"mcpServers": {
  "mpu_ro": {"type": "http", "url": "http://127.0.0.1:7337/ro",
             "headersHelper": "mpu mcp token"},
  "mpu_rw": {"type": "http", "url": "http://127.0.0.1:7337/rw",
             "headersHelper": "mpu mcp token"}}}
```

- [ ] **Шаг 4: добавить правила разрешений**

```
allow: ["mcp__mpu_ro__*"]     ask: ["mcp__mpu_rw__*"]
```

Старые 48 правил `Bash(mpu …)` пока не трогать: 249 команд ещё на маршруте `legacy` и вызываются через Bash.

- [ ] **Шаг 5: приёмка**

- [ ] `mpu xlsx get …` в терминале печатает байт-в-байт как до перебора (12 golden зелёные);
- [ ] `/mcp` показывает `mpu_ro` и `mpu_rw` подключёнными;
- [ ] в сессии видны `mcp__mpu_ro__xlsx_get`, `mcp__mpu_ro__xlsx_ls`, `mcp__mpu_ro__xlsx_resolve`;
- [ ] вызов `mcp__mpu_ro__xlsx_get` возвращает `structuredContent` по схеме и **не спрашивает** подтверждения;
- [ ] `mcp__mpu_rw__xlsx_alias_add` **спрашивает** подтверждение;
- [ ] `mcp__mpu_ro__xlsx_alias_add` отсутствует (профили не пересекаются);
- [ ] `mpu sql-ro <селектор> "select 1"` работает через Python;
- [ ] `mpu help` содержит `sql-ro`, `tools/list` — нет.

## Долги, вскрытые по ходу

Не блокируют MVP, но записаны, чтобы не потеряться.

- **Две спеки переросли сигнальный порог** правил канала («длиннее ~200 строк — резать или дробить»): `registry.md` — 209 строк, `xlsx.md` — 206. У `xlsx.md` причина структурная: канал предписывает дробить команду с подкомандами на самодостаточные спеки, а она описывает пять подкоманд одним файлом. Дробить — отдельной задачей, не под MVP.
- **Комментарий «зачем» у зависимости `@zod/zod` ссылается на брокера** — следствие того, что `ts/CLAUDE.md` ещё не переписан. Поправить в Task 10.
- **Тестовые копии фикстур живут отдельно от канала** (`ts/src/xlsx/testdata/` против `ts/docs/specs/fixtures/xlsx/`), и расхождение между ними ничем не ловится. Task 6 синхронизирует их руками; проверку синхронности стоит сделать тестом.

## Не входит в MVP

SSE и `notifications/progress`; `subscriptions/listen` и `tools/list_changed`; MRTR и elicitation; `x-mcp-header`; resources и prompts; юнит `systemd --user`; перенос остальных 249 команд; удаление каталога `go/` и устаревших документов `mpu/docs/` (отдельной задачей после того, как MVP докажет конструкцию).
