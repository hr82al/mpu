# Проверка раздела «Язык строки — инварианты» (2026-09-23, sonnet)
Задача спецификатору: новая команда `wb-stocks` (+ пишущая `reset`).
Без раздела: позиционный клиент, `--format csv`, флаг `--sql`, без `ask` — всё по-старому.
С разделом (v1): `target:`, `since:`, вариант `dry`, `end csv`, `ask` первым, справка по порядку; промах — склад в `id:` (агент растянул словарь, чтобы не заводить ключ вне него).
Правка п. 1 («id — номер главной сущности; для прочего — свой ключ с причиной; словарь не растягивать»).
С разделом (v2), другая задача `kiten attach`/`kiten files`: `id:` верно, `file:` вне словаря с причиной, вариант `check`, `ask` только у записи, справка по порядку; мелочь — `end as json` (громкий отказ).
