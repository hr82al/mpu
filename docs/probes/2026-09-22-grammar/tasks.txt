Задачи. На каждую — ровно один вызов тула `mpu`: массив слов JSON в одной строке, без пояснений. Вызвать тул физически нельзя — только напиши, какой массив передал бы с первой попытки.
1. Покажи карточку Kaiten 123.
2. Покажи карточку 123 в markdown.
3. Read-only SQL `select count(*) from wb_cards` у клиента «Ромашка», результат в JSON.
4. Пишущий SQL `update t set x = 1` на сервере sl-1.
5. Узнай, какие аргументы принимает kiten comment.
6. Оставь на карточке 55 комментарий, текст которого — ровно слово `end`.
7. Список моих карточек Kaiten в CSV.
8. Пробный прогон (ничего не меняя) пересчёта витрин клиента 54.
9. Одной строкой прокомментируй первую из моих карточек текстом «готово».
10. Логи сервера sl-1 за последний час через portainer.
11. Найди клиента, в имени которого есть слово «json».

После ответов — отдельным разделом «Где сомневался»: по номерам задач, что было неочевидно и какую форму ты бы попробовал второй.
