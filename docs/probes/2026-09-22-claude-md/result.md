# Проверка раздела «Синтаксис языка — решает слепой замер» (2026-09-22)
Два свежих агента sonnet-спецификатора, задача «сортировка по убыванию и группировка».
С разделом: план по нему — проходимость в трёх оболочках, генерация ru/en, слепой замер 9/10, разбор ошибок «по пропуску / вопреки примеру», docs/probes; сам добавил «непокрытые развилки — владельцу».
Без раздела: синтаксис на вкус (`sort: :created reverse`, `group: :column` — `:x` у нас параметр блока), без проверки оболочек и замера.
Вывод: раздел меняет поведение в нужную сторону, лишнего не тянет.
