Задачи — одной строкой, массивом слов JSON, без пояснений (10 — текст файла):
1. Названия моих карточек в колонке review.
2. Количество моих карточек в колонке done.
3. Каждой моей карточке в колонке review — комментарий «нужен ревьюер».
4. id трёх самых старых моих карточек (по created).
5. Если моих карточек в review больше 5 — напечатать «перегруз».
6. Для каждого клиента из поиска «ромаш» — `select count(*) as n from wb_cards` и напечатать `n` первой строки.
7. Сохранить мои карточки в переменную, напечатать количество и название первой.
8. Определить у kiten метод `stale` (без параметров): мои карточки, созданные раньше 2026-09-01; вызвать и напечатать количество.
9. Сумма поля total по `select total from orders` клиента 54.
10. Файл программы с параметром `col`: комментарий в начале; каждой моей карточке в колонке @col — комментарий «проверь, пожалуйста». И строка запуска с col = review.
Затем «Где сомневался».
