Задачи — одной строкой, массивом слов JSON, без пояснений:
1. Для каждого клиента из поиска «ромаш» — `select count(*) as n from wb_cards` и напечатать `n` первой строки.
2. Для каждой моей карточки в колонке review — напечатать количество её комментариев (`kiten card id: <id>` отдаёт запись с полем `comments` — коллекцией).
3. Мои карточки, созданные раньше 2026-09-01, — количество.
4. Для каждого клиента из поиска «ромаш» — если `select count(*) as n from wb_cards` даёт n больше 1000, напечатать имя клиента (`@x name`).
5. Названия моих карточек, в названии которых есть bug, отсортированные по created.
6. Каждой моей карточке в колонке done, созданной раньше 2026-08-01, — комментарий «архивирую».
Затем «Где сомневался».
