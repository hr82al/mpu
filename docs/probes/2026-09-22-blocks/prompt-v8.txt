Ничего не исследуй, файлов не читай, инструментов не вызывай — ответь только из текста ниже.

Описание тула `mpu`:
---
mpu — Smalltalk на словах. Вход — массив слов, одинаково в параметрах bash/fish/nu без кавычек и в файле программы.
- Унарное — голое слово: `kiten ls`. Ключевое — `ключ: значение` ≡ `--ключ значение`. Ключи подряд — ВСЕГДА одно сообщение.
- Приоритет: унарное выше ключевого. Значение ключа — литерал, `@переменная` или `^текст из слов^`, плюс унарные ему: `id: @c id`.
- `end` закрывает выражение от начала строки (или от начала блока); слово после `end` — сообщение результату. Цепочка: `kiten ls where: column is: review end size`, `kiten ls sortBy: created end first: 3 end json`.
- Блок: `do :a :b … done` (как в bash); внутри блока `end` работает как в начале строки: `each: do :x x name print . sql-ro target: @x id sql: ^…^ end first n print done`.
- Переменная: `x := kiten ls . x size print . x first title print` — выражения разделяет `.` (не `;`). В начале выражения переменная и параметр блока — без `@` (`c title print`), в значении ключа — с `@` (`id: @c id`): там голое слово — текст.
- Поле записи — унарным через пробел: `@c title`, `@c id` (не `@c.id`).
- Коллекция: `size`, `first`, `first: n`, `where: <поле> is:|less:|greater:|includes: <значение>`, `sortBy: <поле>`, `each: <блок>`, `collect: <блок>`, `select: <блок>`, `inject: <нач> into: <блок :acc :x>`.
- Числа и текст: `greater:`, `less:`, `equals:`, `plus:`, `includes:`; булево: `ifTrue: <блок>` (можно одно), `ifFalse:`, `ifTrue:ifFalse:`; `timesRepeat:`. `print` печатает.
- Текст из нескольких слов — `^готово к ревью^`; комментарий — `rem … end`; служебное слово как литерал — `-- end`.
- Запись (комментарий, SQL на запись, `define:`) — строка начинается словом `ask`, ОДИН раз, первым словом, даже если запись внутри блока: `ask kiten ls where: column is: review end each: do :c kiten comment id: @c id text: ping done`.
- Метод навсегда: `ask kiten define: cardsIn purpose: ^мои карточки в колонке^ do :col kiten ls where: column is: @col done`.
- Программа из файла: `mpu run: файл.mpu col: review`, внутри `@col`. `target:` — клиент по номеру/имени/части; `sql-ro target: … sql: ^…^`; `search query: …`.
---

Задачи — одной строкой, массивом слов JSON, без пояснений:
1. id моих карточек в колонке done, созданных раньше 2026-09-01.
2. Для каждой моей карточки в колонке review — напечатать название и количество её комментариев (`kiten card id: …` отдаёт запись с полем `comments`).
3. Если у клиента 54 `select count(*) as n from orders` даёт n меньше 10 — напечатать «мало заказов».
4. Максимальное created среди моих карточек (через `inject:into:` и `greater:`).
5. Каждой моей карточке, в названии которой есть bug, — комментарий «проверь баг».
6. Сохранить мои карточки в переменную; напечатать названия тех, что в колонке review, и затем общее количество.
7. Для чисел от 1 до 3 (`1 to: 3 do: <блок :i>`) напечатать `i`.
8. Определить у kiten метод `inColumn` с одним параметром — мои карточки в этой колонке; вызвать с done и напечатать количество.
9. Названия первых 5 моих карточек, отсортированных по created, — в JSON.
10. Для каждого клиента из поиска «ромаш» — если его `select count(*) as n from wb_cards` n больше 1000, оставить мне в Telegram сообщение с именем клиента (`telegram send to: me text: …`).
Затем «Где сомневался».
