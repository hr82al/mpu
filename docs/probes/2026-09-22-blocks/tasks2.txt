Задачи — каждая ОДНОЙ строкой, массивом слов JSON, без пояснений; вызвать тул нельзя, напиши, что передал бы с первой попытки.
1. Напечатать название каждой моей карточки Kaiten.
2. Список id моих карточек в колонке «review».
3. Если у клиента 54 в таблице wb_cards больше 1000 строк (`select count(*) as n from wb_cards`), напечатать «big», иначе «small».
4. Для каждого клиента, в имени которого есть «ромаш», получить `select count(*) as n from wb_cards` и вернуть коллекцию этих чисел.
5. Сохранить мои карточки в переменную, затем напечатать их количество и название первой.
6. Каждой моей карточке в колонке «review» оставить комментарий «ping».
7. Определить у `kiten` постоянный метод `cardsIn:` — мои карточки в колонке-параметре; затем вызвать его с «done» и вернуть количество.
8. Сумма поля `total` по строкам `select total from orders` клиента 54.
9. Напечатать «tick» пять раз.
10. Определить у `kiten` метод `cardsIn:since` (колонка и дата, поле карточки `created`), вызвать с «review» и 2026-09-01.
Затем «Где сомневался»: по номерам.
