Ничего не исследуй, файлов не читай, инструментов не вызывай — ответь только из текста ниже.

Грамматика mpu (слова; одинаково в параметрах bash/fish/nu без кавычек и в файле программы, который делится по пробелам):
- Унарное — голое слово: `kiten ls`. Ключевое — `ключ: значение` или `--ключ значение`; ключи подряд — одно сообщение.
- Блок `do :x … end`; переменная `@x`; `x := …`; выражения через `.`; непарный `end` закрывает от начала строки, слово после — результату (`kiten ls end json`).
- Слово грамматики как литерал — через `--` перед ним: `text: -- end`.
- Кавычки ' и " не используются: оболочка их снимает. Символы ( ) [ ] { } < > | ; $ & # * ? писать нельзя.
- Комментарий: `rem <слова> end` — всё до `end` игнорируется; слово `end` внутри комментария — `-- end`.
- `kiten comment id: <номер> text: <текст>`; `kiten ls`; запись карточки: `@c id`, `@c title`, `@c column`; `where: <поле> is: <значение>`; `do:`/`collect:` у коллекций; `print`.
- Строки с записью начинаются словом `ask` (один раз, в начале всей строки или программы).
- Программа из файла: `mpu run: <файл> [<ключ>: <значение> …]`, параметры внутри — `@ключ`.
- Текст из нескольких слов — между словами `str` и `end`: `text: str готово к ревью, проверь тесты end`. Одно слово — можно без `str`. Слово `end` внутри текста — `-- end`.

Задачи — каждая одной строкой, массивом слов JSON (как параметры после `mpu`), без пояснений; задача 8 — текст файла программы целиком (как он лежал бы в файле).
1. Оставить на карточке 55 комментарий «готово к ревью, проверь тесты».
2. Оставить на карточке 55 комментарий «ok».
3. Оставить на карточке 55 комментарий «the end is near».
4. Каждой моей карточке в колонке review оставить комментарий «ping от бота».
5. Напечатать названия моих карточек; в строке — комментарий «список для стендапа».
6. Оставить на карточке 55 комментарий «цена 100% и a=b».
7. Оставить на карточке 55 комментарий «см. раздел end-to-end».
8. Файл программы: принимает параметр `col`; комментарий в начале «рассылка по колонке»; каждой моей карточке в колонке @col — комментарий «проверь, пожалуйста». И строка запуска этого файла с col = review.
Затем «Где сомневался».
