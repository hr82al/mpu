# Слепой замер описания тула `mpu` после установки 158–165 (2026-09-23, sonnet)
Установленное описание (v3 + фраза про refusal.hint): 5/10 — вариант после ключа (нет примера строки), отбор (`end size`, `where:`) и `stdin` не описаны.
v4 (сжато, примеры на провалы, проверены на установленном mpu): 8/10 — `markdown` вместо `md`, `end title` вместо `pick: title`.
v5 (+ `end md`, `pick:` с пояснением «поле всех / первой»): набор 2 — 10/10, набор 1 — 10/10. Порог 9/10 на двух наборах подряд взят.
