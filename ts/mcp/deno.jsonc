{
  // Подпроект `mcp` рабочей области `ts/` (`docs/specs/platform/mcp-objects.md`):
  // переводчик MCP ↔ `mpu-back`. Импорты и задачи — у корня
  // (`../deno.jsonc`), здесь только своё.
  "fmt": {
    // Голдены сверяются байт-в-байт с каналом спецификаций.
    "exclude": ["src/testdata"]
  }
}
