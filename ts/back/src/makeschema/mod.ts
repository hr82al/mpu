/**
 * Создание схемы клиента в локальном стенде
 * (`docs/specs/make-schema.md`): единственная команда с локальным
 * транспортом.
 *
 * Наружу модуль отдаёт только команду реестра; сборка docker-вызова
 * остаётся внутренностью.
 */

export { makeSchemaCommand } from "./cmd_make_schema.ts";
