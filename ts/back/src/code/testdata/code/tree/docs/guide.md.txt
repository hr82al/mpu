# Руководство дерева-фикстуры

Дни считает `src/days.ts`. Прежний адрес `src/gone.ts` не существует —
на нём проверяется вопрос «путь упомянут в документации, но не в коде».
Сетку строит `src/gone.ts`.
