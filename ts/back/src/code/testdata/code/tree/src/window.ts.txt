// Побайтовый близнец №1. Имя своё, тело общее с grid.ts и seed_test.ts.
import { addDays } from './days';

export function windowDays(from: string, to: string): string[] {
  const days: string[] = [];
  for (let day = from; day <= to; day = addDays(day, 1)) days.push(day);
  return days;
}
