// Файл существует и разбираем, но программы у проекта нет: конфигурация
// наследует пакет, которого нет. Отказ обязан быть назван, а не подменён
// пустым перечнем.
export function alpha(day: string): string {
  return day;
}
