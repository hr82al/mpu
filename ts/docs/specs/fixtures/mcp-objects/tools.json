{
  "tools": [
    {
      "name": "help",
      "description": "Справка mpu: какие команды есть и как их писать. Звать перед тулом mpu, когда команда или её аргументы не известны.\n\npath — путь до узла дерева команд: [] или без path — справка корня (список команд верхнего уровня), [\"kiten\"] — группа, [\"kiten\", \"card\"] — команда. Справка никогда не исполняет команду.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "путь до узла дерева команд; по умолчанию — корень"
          }
        }
      },
      "annotations": {
        "readOnlyHint": true
      }
    },
    {
      "name": "mpu",
      "description": "mpu — Smalltalk на словах к mpu-back. Вход — массив слов `words`.\n\n- Голое слово — сообщение: `kiten ls`. Значение — только ключом: `ключ: значение` ≡ `--ключ значение`; одно слово массива — одно значение, пробелы можно: `sql: \"select 1\"`.\n- Порядок: команда → варианты → ключи → `end` → сообщения результату. Вариант — слово ДО ключей: `process dry target: 54`, `logs portainer target: sl-1 since: 1h`.\n- `end` закрывает выражение; слово после — результату: формат (`end json`, `end md`; список — `<команда> formats`) или отбор: `kiten ls end size`, `kiten ls where: column is: review end pick: title` (поле всех), `… end first title` (поле первой), `end first: 3`, `where: <поле> is:|less:|greater:|includes: <значение>`.\n- Ключи везде одни: `target:` где (клиент по номеру/имени/части, `sl-N`, `dev:N`), `id:` номер сущности, `text:` текст, `query:` поиск, `since:`/`until:` время, `limit:` сколько.\n- Значение из команды — группой: `id: do kiten ls end first id`; из stdin — `sql: stdin`.\n- `it` — последний результат без повторного запроса: `it end json`.\n- Запись или отправка — строка начинается `ask`: `ask sql target: sl-1 sql: \"…\"`, `ask kiten comment id: 5 text: ok`.\n- Слово грамматики как значение — через `--`: `text: -- end`.\n- Справка — `help` последним: `kiten card help`; ключи — `… keys`, варианты — `… variants`.\n- Отказ несёт `refusal.hint` — исправленную строку `words`: вызови её.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "words": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "слова строки, как в оболочке после «mpu»"
          }
        },
        "required": [
          "words"
        ]
      }
    }
  ]
}
