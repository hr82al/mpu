# Читаемость кода `ts/` — разбор и очередь порций

Документ-разбор: где именно код читается плохо, почему и что с этим делать. Правила, по которым
идёт разбор, живут в `ts/CLAUDE.md` («Порядок работы», «Структура кода», «Имена и комментарии»,
«Образец стиля», «Типы и API»); здесь — доказательная база и очередь работ.

Документ ведёт сессия-спецификатор; спекой он не является и на поведение команд не влияет. Туда же
относится `docs/tools/` — два скрипта замера, которыми получены числа ниже.

Исправленное вычёркивается прямо в тексте находки пометкой **✅** — так видно, что осталось.

---

## 0. Главный вывод

Код `ts/` здоров: пустых `catch` — 0, вложенности глубже 5 — 0, приведений `as T` — 4 на всё дерево,
`!.` — 0, модулей `utils`/`common`/`helpers` — нет. Гипотеза «код писался под декларативные правила
(SOLID/KISS/DRY) и потому переусложнён» **массово не подтверждается**: слоёв ради слоёв в дереве нет,
проблемы точечные.

Что подтверждается — два системных мотива, оба про **границу**, а не про объём:

1. **Экспорт как способ добраться до кода из теста.** Символов, к которым нет ни одной ссылки из
   другого файла исходников, — 190 из 528; из них 99 держатся только тестами. Это не «мёртвый код»:
   почти всё живо и вызывается внутри своего файла. Дефект в другом — публичная граница модуля
   объявлена шире, чем его настоящий контракт, и читатель фасада не знает, что из этого ему можно.
2. **Один порт исполнения на все случаи.** `CommandIo` собирает девять разнородных возможностей, и
   ни один потребитель не сужает его до нужного среза (`Pick<CommandIo, …>` — 0 совпадений в дереве,
   §6). Из того же корня растут три функции-оркестратора на 53–184 строки (§4).

Проверявшаяся гипотеза «функции принимают источник вместо данных, отсюда фейки в тестах»
**не подтвердилась**: подмены в тестах закрывают либо реальный внешний мир, либо узкие
функции-порты, которым передают именно данные (§7). Единственное исключение — `CommandIo` выше.

Оба подтверждённых мотива лечатся не «рефакторингом красоты», а сужением границ — по одному классу
за порцию.

---

## 1. Замеры

Срез `main` от 2026-08-15 (коммит `ae3d1d9`), `ts/src` = 17 261 строка исходников и 21 999 строк
тестов, 528 экспортов в 95 не-тестовых файлах.

| Сигнал | Значение |
| --- | --- |
| Экспортов всего | 528 |
| — нет ссылок из других файлов исходников, есть в тестах | 99 (72 вне `src/kaiten/`) |
| — нет ссылок нигде, кроме собственного файла | 91 (58 вне `src/kaiten/`) |
| Функций длиннее 40 строк | 20 |
| Экспортов без JSDoc | 24 |
| Фабрик (`make*` / `build*` / `create*`) | 10 вне `src/kaiten/` |
| Тестов, держащих фейки/двойники | 36 файлов из 76 |
| Приведений `as T` в исходниках | 4 |
| Пустых `catch` | 0 |

Гейты на этом срезе: `deno lint`, `deno check .`, `deno task test` (596 passed, 932 steps, 0 failed) —
зелёные. `deno fmt --check` по коду (`src main.ts scripts`) чистый; падает только `ts/CLAUDE.md` —
переносы строк после правки правил 15 августа, к коду отношения не имеет.

Числа шире, чем в таблице постановки (115/58/57, 5 длинных, 24 без JSDoc): там был черновой
текстовый скан, он считал ссылкой в том числе строку `import`, поэтому символ, импортированный
и нигде не использованный, выглядел живым. Здесь строки-проводка (`import {…}`, `export {…} from`)
из подсчёта сняты, а потребители вне `src/` (`main.ts`, `scripts/`) добавлены. Подгонять числа под
постановку не нужно — счёт ведётся от таблицы выше.

### Как мерить

Из каталога `ts/`:

```bash
deno fmt --check src main.ts scripts && deno lint && deno check . && deno task test

deno run -A docs/tools/audit_exports.ts > /tmp/exports.tsv   # экспорты и их потребители
awk -F'\t' '$5==0 && $6==0' /tmp/exports.tsv | wc -l          # без потребителей вовсе
awk -F'\t' '$5==0 && $6>0'  /tmp/exports.tsv | wc -l          # держатся только тестами
deno run -A docs/tools/audit_shape.ts | grep -E '^(LONG|NODOC)'  # длинные функции, экспорты без JSDoc

grep -rn 'as [A-Z]' src --include=*.ts | grep -v _test | wc -l   # приведения типов
grep -rn 'catch\s*{\s*}' src --include=*.ts | wc -l              # пустые catch
```

Скан текстовый и намеренно грубый: он даёт **кандидатов**, каждый из которых проверяется открытием
кода. Все находки ниже так и проверены; отклонённые кандидаты вынесены отдельно в §7 — чтобы
следующий проход не начинал с них заново.

---

## 2. Экспорт по инерции: публичная граница шире контракта

Класс: символ живёт внутри своего файла, но объявлен `export`, а иногда и вынесен фасадом наружу.
Читатель фасада не может отличить такой символ от настоящего контракта; при правке приходится
считаться с несуществующими потребителями. Правка одинаковая — **снять `export`, сам символ
оставить на месте**.

| Адрес | Что | Почему находка |
| --- | --- | --- |
| `src/registry/mod.ts:35` | `type BareHandler` | тип поля `CommandGroup.bare`; `entrypoint/mod.ts` зовёт `group.bare(...)` через `CommandGroup`, имя типа ему не нужно |
| `src/command/schema.ts:36` | `class SchemaShapeError` | фасад `command/mod.ts` его не реэкспортирует, `instanceof` по нему никто не делает — export держит контракт, которого нет |
| `src/mcp/jsonrpc.ts:60` | `interface RpcError` | наружу отдаётся готовый `RpcBody`, поле `.error` читают структурно |
| `src/mcp/tool.ts:39` | `interface ToolAnnotations` | фасад `mcp/tools.ts:27–34` реэкспортирует `Tool`/`ToolCallResult`/`ToolEntry`, но не его |
| `src/mcp/server.ts:54` | `function createMcpApp` | JSDoc обещает «то же, что слушает порт, но вызываемое напрямую»; единственный вызов — `serveMcp` (`server.ts:121`), ни один тест приложение напрямую не поднимает |
| `src/legacy/mod.ts:27` | `const LEGACY_BIN_KEY` | читается один раз, в `legacy/mod.ts:84`; тот же паттерн у соседа (`PORT_KEY`, `mcp/cli.ts:50`) намеренно не экспортирован |
| `src/loki/mod.ts:41` | `interface HostService` | потребитель (`init/cmd_init.ts:592`) читает только `series.pairs.length`, элемент по типу не называет |
| `src/update/sync.ts:83` | `class PgTimeoutError` | конструируется только как причина `abort()` внутри файла; снаружи `instanceof` никто не делает |
| `src/update/sync.ts:255` | `function fanOutNumbers` | единственный вызов — `sync.ts:152`; вынесен фасадом `update/mod.ts` в публичную поверхность |
| `src/update/cmd_update.ts:46` | `const DEFAULT_UPDATE_LIMITS` | внутренний фолбэк `options.limits ?? …`; тесты во всех вызовах передают свои `limits` явно |
| `src/sqlro/target.ts:26` | `const SW_ALIASES` | деталь реализации `routeOf` (`target.ts:62`), больше нигде не встречается |
| `src/kiten/cmd_card.ts:68` | `type CardOutputView` | тип приватной `viewOf` (`cmd_card.ts:102`), при этом вынесен фасадом `kiten/mod.ts:8` |

Из 58 кандидатов этого класса (вне `src/kaiten/`) находками оказались 12. Остальные 46 — типы в
сигнатурах экспортированных функций: потребитель обязан иметь возможность назвать тип, даже если
сегодня собирает значение структурно. Это норма, повторно их не проверять (§7).

---

## 3. Публичный API, расширенный ради теста

Класс: символ не нужен ни одному файлу исходников — его держит тест, которому удобнее позвать
внутреннюю функцию напрямую. Правило `ts/CLAUDE.md`: поведение проверяется через то, что и так
экспортировано.

- `src/xlsx/range.ts:94` `colLetters` — вызывается только из `cellName` (`range.ts:119`) и из теста
  (`range_test.ts:178`, round-trip с `parseColLetters`). Публичная точка — `cellName`.
  **Правка:** снять `export`, проверку перенести на `cellName` / уже публичный `parseColLetters`.
- `src/config/mod.ts:17` `EMPTY_STORE` — в тесте это только ожидаемое значение
  (`assertEquals(parseStore(undefined), EMPTY_STORE)`), а литерал `{ values: {}, aliases: {} }`
  самодокументируем. **Правка:** снять `export`, в тесте — литерал.
- `src/registry/mod.ts:117` `OWN_COMMANDS` — единственный символ дерева без ссылок даже внутри
  своего файла: список `["mcp"]` живёт ради `tree_test.ts`, который тут же фиксирует его вторым
  литералом (`tree_test.ts:64`). **Правка:** снять `export`, список объявить в контракт-тесте.
- `src/portainer/mod.ts:225` `fetchPortainerJson` — общая GET+JSON-обвязка; тесты зовут её с тем же
  путём `/api/endpoints`, который зашит в `listEndpoints` (`portainer/mod.ts:83`). Экспорт открывает
  второй способ дойти до тех же веток ошибок. **Правка:** снять `export`, тесты — на
  `listEndpoints`/`listContainers` (параметр `timeouts` у обеих уже есть).
- `src/xlsx/workbook.ts:82` `parseWorkbookParts` — **экспорт оставить**: 15 тестов на границы формата
  дешевле гонять без сборки zip. Дефект не в экспорте, а в JSDoc: «отдельная точка входа для тестов
  граничных случаев» — это признание, а не контракт. **Правка:** переписать JSDoc в контракт
  («разбирает книгу из уже распакованных частей архива»), тесты не трогать.

---

## 4. Длинные функции

Порог `ts/CLAUDE.md` — 40 строк. Кандидатов 17 (вне `src/kaiten/`), находок 3. Остальные 14 длинны
**декларацией, а не логикой**: это фабрики, возвращающие объект с методами по 1–15 строк
(`runtime/mod.ts:233` `makeDenoIo`, `command/mod.ts:282` `defineCommand`, `testing/mod.ts:17`,
`env/mod.ts:82`, `store/mod.ts:24`, `logs/cache.ts:41`, `invokelog/mod.ts:110`), либо исчерпывающий
`switch` (`entrypoint/help_command.ts:82`), либо неразделимая идиома работы с ресурсом
(`http/mod.ts:258`, `update/sync.ts:137`). Резать их — ухудшить чтение, а не улучшить.

- `src/init/cmd_init.ts:338` `runInit` — **184 строки** (338–521), самая длинная функция дерева.
  Шаги названы в самих комментариях: bootstrap (343), фильтрация (356–359), параллельный сбор
  (361–374), разбор исходов (376–433), транзакция записи (435–494), применение warmup (496–503).
  Дороже менять: новый шаг спеки вклинивается в середину, а не прибавляется в конец.
  **Правка:** выделить `initializeEndpoints`, `collectContainers`, `recordStand`, `applyWarmup`;
  `runInit` остаётся конвейером на 30–40 строк.
- `src/entrypoint/mod.ts:100` `runCli` — **140 строк** (100–239). Шесть последовательных фаз:
  режим дополнения (117–123), разбор `--json` (125–134), справка/версия (135–166), маршрут
  (169–173), диспетчеризация (174–222), обработка ошибок (223–238).
  **Правка:** выделить `runCompletion`, `handlePreCommands`, `dispatchCommand`.
- `src/command/args.ts:42` `parseArgv` — **53 строки**. Разбор длинного флага (66–84) — законченный
  кусок с собственной оговоркой про отрицательную форму (`--no-images`).
  **Правка:** выделить `parseLongFlag`; после этого тело ~35 строк.
  Уточнение к отчёту разбора: заявленное «4 уровня вложенности» **не подтвердилось** — счастливый
  путь идёт по левому краю через `continue`, глубина 3, правило не нарушено. Основание находки —
  только длина, приоритет ниже двух предыдущих.

---

## 5. JSDoc на экспортах

Кандидатов 24: пять находок ниже, шестой (`config/mod.ts:17` `EMPTY_STORE`) закрыт в §3 снятием
экспорта, остальные 18 документированы блоком модуля сверху либо блоком над соседней перегрузкой —
ложные срабатывания сканера.

| Адрес | Контракт, которого не хватает |
| --- | --- |
| `src/portainer/mod.ts:79` `listEndpoints` | список окружений Portainer с их статусом доступности |
| `src/portainer/mod.ts:91` `listContainers` | контейнеры одного окружения; имена приходят как их отдаёт Docker, с ведущим `/` |
| `src/logs/sources.ts:55` `readLokiOverHttp` | реализация `ReadLoki` поверх HTTP: записи окна из Loki |
| `src/logs/sources.ts:58` `listContainerNamesOverHttp` | реализация `ListContainerNames`: имена контейнеров окружения, ведущий `/` снят |
| `src/logs/sources.ts:68` `readContainerLogsOverHttp` | реализация `ReadContainerLogs`: поток логов контейнера, разобранный из формата Docker |

Три последних — реализации именованных портов; JSDoc здесь отвечает на вопрос «чем эта реализация
отличается от типа», а не пересказывает тело.

---

## 6. Открытый вопрос: `CommandIo` целиком там, где нужны два поля

Не порция, а **решение, которое принимает пользователь** — правка сквозная и меняет сигнатуры
команд.

`src/command/mod.ts:68` `CommandIo` собирает в одну сущность файлы, stdin/stdout, конфиг-хранилище,
токен, shell, legacy-раннер, env-файл, кэш-БД и `progress`. Обработчики принимают его целиком:
`grep -rn 'io: CommandIo' src | grep -v _test` — 51 совпадение, `grep -rn 'Pick<CommandIo' src` — 0.
Правило `ts/CLAUDE.md` («принимать интерфейсы… ровно тот минимум полей, который используешь») и
собственный образец проекта (`EnvKeys = Pick<EnvFile, …>`, `ts/CLAUDE.md:66`) применены к `EnvFile`,
но не к `CommandIo`. Следствие видно в тестах: универсальный двойник `makeFakeIo` с заглушками
«упасть при касании» на все поля плюс два самодельных двойника (`registry/contract_test.ts:498`,
`init/cmd_init_test.ts:97`) — ровно то расхождение, которое `makeFakeIo` и должен был убрать.

Против правки: `CommandIo` объявлен спекой `docs/specs/platform/command-contract.md` как порт
исполнения команды; сужение до `Pick<…>` в 51 месте — большой диффуз, а второй реализации порта нет
и не предвидится. За правку: сигнатура перестаёт врать о том, что команда трогает, и двойник в
тесте собирается под конкретный срез.

Развилка: (а) не трогать, зафиксировать как осознанное отступление в `ts/CLAUDE.md`; (б) сузить
точечно — там, где команда использует ≤3 поля; (в) сузить везде отдельной порцией-переукладкой.
Порядок при (б)/(в): сперва правка спеки `command-contract.md`, потом порция на код.

---

## 7. Проверено и отклонено

Чтобы следующий проход не начинал с того же места.

- **Фейки в тестах — дефекта нет, 0 находок.** Проверены 13 файлов из 36: топ по числу упоминаний
  `fake`/`stub` (6–68), включая все четыре, названные в постановке; у остальных 23 упоминаний ≤6 и
  те же два разобранных приёма (сервер на loopback либо общий `makeFakeIo`). Подмены закрывают
  ровно два класса: реальный внешний мир (HTTP-сервер на loopback, sqlite, временная ФС) и узкие
  функции-порты протокола (`ReadLoki`, `OpenPgSession`, `OpenReadOnlySession`, `OpenClient`,
  `EnvFileStore`), которые тест подменяет **чистыми функциями данных**, а не многометодными
  клиентами. Места, где функции протаскивали бы источник ради двух значений, в коде уже разобраны
  (`requirePortainerAccess` берёт `{ get }`, `LogsOptions` — функции).
- **Фабрики (10) — 1 находка из 10** (`createMcpApp`, учтена в §2). `makeDenoIo`, `makeDenoOutput`,
  `makeEnvFileStore`, `makeInvokeLog` — точка сборки (`main.ts:17,18,31`), им там место.
  `makePgOpener` — граница с недоступной в тестах БД. `buildLogQl`/`buildMultipartBody` — не
  фабрики вовсе: собирают данные, а не реализации.
- **Интерфейсы с «одной реализацией»**: `InvokeLog` имеет вторую настоящую (`NO_INVOKE_LOG`,
  null-object, `invokelog/mod.ts:80–88`); `Output` в проде оборачивается декоратором
  (`record.capture`, `main.ts:31`); `EnvFileStore` отделяет политику от диска и этим снимает
  измеримую боль. Все три — норма.
- **44 экспортированных типа** из класса §2 — участники сигнатур публичных функций, снимать
  `export` нельзя.
- **Экспорты, объявленные спеками**: `selectQuery`, `outcomeOf`/`outcomeAt`/`dbError`/`serverText`/
  `toValue` (`docs/specs/sql-ro.md`), `runIdOf`, `maskJsonText`, `LOCK_NAME`, `DEFAULT_*`
  (`docs/specs/platform/invoke-log.md`), `isAliasLike` (`docs/specs/xlsx.md`), `syncClient` и
  `ClientNotFoundError` (потребитель назван в шапке `update/mod.ts`).

---

## 8. Очередь порций

Порции режутся по классу правки, не по каталогам: у каждой одно основание, свой критерий приёмки и
зелёные гейты. Смешивать снятие экспорта с резкой длинных функций нельзя.

**0. Гигиена гейта (одна строка).** `deno fmt --check` падает на `ts/CLAUDE.md` — переносы строк
после правки правил 15 августа. Прогнать `deno fmt ts/CLAUDE.md`. Приёмка: `deno fmt --check` без
аргументов зелёный.

**A. Снять экспорт по инерции — 12 адресов §2.** Один вид правки, поведение не меняется.
Приёмка: перечисленные символы больше не `export`, из фасадов (`update/mod.ts`, `kiten/mod.ts`)
удалены соответствующие строки реэкспорта; гейты зелёные; `audit_exports.ts` — число «нет ссылок
нигде» падает с 91 до ≤79.

**B. Дописать JSDoc — 5 адресов §5.** Контракт, не пересказ тела. Приёмка: `audit_shape.ts` NODOC
падает с 24 до ≤19; гейты зелёные.

**C. Убрать экспорты ради тестов — 4 адреса §3 + JSDoc `parseWorkbookParts`.** Единственная порция,
которая трогает тесты; поведение не меняется, число тестов не уменьшается. Приёмка: `colLetters`,
`EMPTY_STORE`, `OWN_COMMANDS`, `fetchPortainerJson` не `export`; тесты, которые их звали, проверяют
то же через публичные функции; `deno task test` — 596 passed, 0 failed.

**D. Разрезать `runInit`** (`init/cmd_init.ts:338`, 184 строки). Отдельная порция: самая большая
функция дерева, риск выше остальных. Приёмка: `runInit` ≤ 40 строк, выделенные части названы по
шагу спеки, `audit_shape.ts` LONG падает на 1, `cmd_init_test.ts` не переписан (сигнатура команды
не меняется).

**E. Разрезать `runCli`** (`entrypoint/mod.ts:100`, 140 строк). Приёмка та же по форме; сигнатура
`runCli` не меняется — её зовёт `main.ts:20`.

**F. Разрезать `parseArgv`** (`command/args.ts:42`, 53 строки) — по остаточному принципу, основание
только длина. Приёмка: тело ≤ 40 строк, `parseLongFlag` покрыт существующими тестами разбора.

Порядок: 0 → A → B → C → D → E → F. Сначала то, что сужает поверхность (дёшево и без риска), затем
правки формы. После каждой порции — дельта замеров §1; числа только убывают.
