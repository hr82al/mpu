# Глоссарий — термин → имя в TS-коде

Изолированные Deno-сессии не видят кода друг друга; без глоссария каждая
переведёт термины по-своему. Новый термин — новая строка.

| Термин             | Имя в коде            | Пояснение                                                                                   |
| ------------------ | --------------------- | ------------------------------------------------------------------------------------------- |
| реестр команд      | `registry`            | единый список команд: маршрутизация + справка                                               |
| точка входа        | `entrypoint`          | режим вызова команды: CLI для человека или MCP-сервер для агента                            |
| маршрут            | `route`               | способ исполнения команды: `native` (код на TS) или `legacy` (подпроцесс Python-реализации) |
| политика           | `policy`              | объявленный класс команды: `ro` — только читает, `rw` — мутирует                            |
| профиль            | `profile`             | набор тулов одной политики, публикуемый MCP-сервером (`ro` или `rw`)                        |
| тул                | `tool`                | команда маршрута `native`, опубликованная в MCP                                             |
| инструкции профиля | `profileInstructions` | текст, объясняющий клиенту, для каких задач искать тулы этого профиля                       |
| схема аргументов   | `argsSchema`          | описание входа команды; из него строятся и разбор argv, и `inputSchema` тула                |
| схема результата   | `resultSchema`        | описание выхода команды; из него строится `outputSchema` тула                               |
| рендер             | `render`              | преобразование результата команды в текст для человека                                      |
| токен доступа      | `accessToken`         | статический Bearer для MCP-сервера; файл 0600                                               |
| конверт результата | `RESULT_COMPLETE`, `LISTING_CACHE` | поля, которыми результат называет свою полноту и кэшируемость; без признака полноты клиент ревизии `2026-07-28` бракует ответ целиком |
| признак полноты | `resultType` | выдача отдана целиком; частичных у сервера нет |
| срок годности результата | `ttlMs` | сколько миллисекунд клиенту разрешено держать результат в кэше; у нас постоянный ноль |
| область кэша | `cacheScope` | кому принадлежит закэшированный результат; у нас `private` — спросившему |
| результат-перечень | `ToolsListResult`, `DiscoverResult` | результат, которому конверт добавляет срок годности и область кэша; у `tools/call` их нет |
| селектор | `Selector` | строка адресации: `sl-N`, client_id, email, IP, spreadsheet_id, sid, заголовок |
| номер сервера | `ServerNumber` | `N` из `sl-N`; main-сервер — `sl-0`, особых веток не имеет |
| клиент | `Client`, `ClientId` | арендатор системы; схема клиента в PG — `schema_<client_id>` |
| кандидат | `Candidate` | строка выдачи резолва селектора (клиент/таблица/сервер) |
| таблица | `Spreadsheet`, `SpreadsheetId` | Google-таблица клиента |
| sid | `Sid` | идентификатор кабинета WB |
| кэш-БД | `CacheDb` | локальная служебная SQLite-БД (`~/.config/mpu/mpu.db`); атом — `platform/store.md` |
| локальные настройки | `Store` | JSON-файл предпочтений `~/.config/mpu/config.json` (`platform/config.md`); имя занято настройками исторически — кэш-БД его не использует |
| env-файл | `EnvFile` | `~/.config/mpu/.env`, слой секретов/подключений |
| журнал вызовов | `InvokeLog`, `RunId` | одна запись на вызов команды; `run_id` = время+pid |
| dev-стенд | `Dev` | отдельная нода; одна PG-БД на всех клиентов |
| read-only сессия | `ReadOnlySession` | PG-сессия с серверным запретом записи |
| инстанс | `Instance` | сервер `sl-N` (N > 0) с per-client схемами; main — `sl-0` |
| оркестратор | `Portainer` | Portainer API — реестр endpoint'ов и контейнеров стенда |
| контейнер | `Container` | Docker-контейнер стенда; `mp-sl-<N>-cli` — CLI-контейнер сервера N |
| discovery | `Discovery` | обход Portainer (endpoints → контейнеры) с записью в кэш-БД |
| снапшот кэша | `Snapshot` | полная перезапись клиентских таблиц кэш-БД одной транзакцией |
| точечный синк | `ClientSync` | upsert одного клиента в кэш-БД без полного снапшота |
| scoped-замена | `scopedReplace` | замена подмножества строк таблицы кэш-БД по ключу области (например, по обойдённым доскам) одной транзакцией |
| TTL-кэш | `TtlCache` | запись кэша с временем годности: протухшая при чтении равнозначна отсутствующей |
| книга | `Workbook` | содержимое `.xlsx` целиком |
| лист | `Sheet` | вкладка книги/таблицы |
| диапазон | `Range` | `Лист!A1:B2`; открытый — без одной из границ |
| алиас | `Alias` | короткое имя пути к `.xlsx` в кэш-БД |
| хост стенда | `Host` | значение Loki-label `host`: `sl-N`, `wb-N`, `dt-N`, `wb-clusters`, `wb-positions` |
| сервис стенда | `ComposeService` | значение Loki-label `compose_service` (`api`, `wb-loader`, …) |
| запись лога | `LogEntry` | единица ответа Loki: наносекундный ts + строка лога |
| meta-блок | `MetaBlock` | служебная шапка SQL-команд в stderr (server/host/БД/search_path/режим/SQL) при `--dry`/`-v` |
| набор строк результата | `ResultSet` | колонки + строки, отданные одним оператором SQL; печатается набор первого оператора вызова |
| БД воркспейсов | `WorkspacesDb` | PG-база sw-back; SQL к ней исполняется изнутри контейнера sw-back (sw-алиасы селектора) |
| транспорт | `Transport` | способ доставки exec: `ssh` \| `portainer`; выбирается per-server |
| таргет | `Target` | адресат выполнения: сервер (`sl-N`/dev) либо контейнер по точному имени |
| Portainer-endpoint | `Endpoint`, `EndpointId` | окружение Portainer; пара (base_url, endpoint_id) адресует Docker API сервера |
| кэш контейнеров | `ContainerCache` | таблица `portainer_containers` в кэш-БД: снапшот discovery всех контейнеров фермы |
| fan-out | `FanOut` | прогон одной команды по списку таргетов (последовательно или параллельно) |
| фоновый запуск | `DetachedRun`, `DetachId` | detach-режим `run-js`; id — 8 hex-символов, скрипт и лог в `/tmp` контейнера |
| one-shot контейнер | `OneShotContainer` | разовый job (`migrations`/`init-`): `Exited (0)` — норма, не сбой |
| лоадер-контейнер | `LoaderContainer` | демон с лоадер-подстрокой в имени — цель tail в `mpu health` |
| карточка | `Card`, `CardId` | карточка Kaiten; web-URL — `{base}/{id}` |
| селектор карточки | `CardRef` | адрес карточки: голый ID или URL (id — последний числовой сегмент пути) |
| пространство | `Space` | пространство Kaiten — контейнер досок |
| доска | `Board` | доска Kaiten внутри пространства |
| дорожка | `Lane` | горизонтальная дорожка доски Kaiten |
| колонка доски | `Column` | колонка доски Kaiten; по её названию выводится этап |
| роль | `Role` | роль компании Kaiten — «тип работ» записей учёта времени |
| этап | `Stage` | канонический шаг конвейера (Очередь…Готово) из названия колонки |
| метка состояния | `StateLabel` | числовой state карточки → `queued`/`in progress`/`done` |
| источник строки | `Source` | почему карточка в выдаче `status`: assigned/time/activity |
| касание | `Touch` | карточка только из ленты действий: не назначена, время не списывал |
| событие ленты | `Activity` | элемент ленты действий пользователя Kaiten: тип действия, момент, карточка на момент действия |
| курсор ленты | `ActivityCursor` | пара (момент, id) последнего элемента страницы — вход следующего запроса ленты; пустая пара = с начала |
| владелец токена | `CurrentUser` | пользователь, которому принадлежит `KITEN_API_KEY`: его id — это «мои» в фильтрах ленты и записей времени |
| вид карточки | `CardView` | плоский объект вывода `kiten card`: те же 21 ключ, что печатает `--json`; источник данных для всех трёх видов |
| вид вывода | `CardOutputView` | какой из трёх видов выбран: `json`, `md`, `pretty` (наглядный терминальный) |
| имена кастомных полей | `PropertyNames` | `id_NNN` → имя поля из справочника компании; пусто — справочник не ответил |
| значение кастомного поля | `PropertyValue` | строка либо массив uid (файловое поле) |
| файл в выводе | `FileView` | подмножество формы «Файл» без `custom_property_id` |
| комментарий в выводе | `CommentView` | комментарий, у которого автор — строка, а не объект |
| имя команды в ошибке | `errorName` | что стоит в префиксе `mpu <имя>: …`; объявляет сама команда |
| справочник Kaiten | `KaitenRefs` | локальный кэш пространств/досок/дорожек/колонок/ролей/кастомных полей в кэш-БД |
| токен-кэш | `TokenCache` | файл `.api-token.json`: JWT sl-back + срок годности, общий для процессов |
| декларативный эндпоинт | `EndpointSpec` | запись реестра `mpu api`: метод + путь + поля тела → команда |
| загрузчик | `Loader` | per-sid конечный автомат wb-loader-app; ровно 25 |
| имя загрузчика | `LoaderName` | camelCase-форма (`wbCards`) — фильтры blocked/resume |
| слаг загрузчика | `LoaderEntity` | kebab-форма (`cards`) — сегмент URL loader-роутов |
| причина блокировки | `BlockedReason` | значение `blocked_reason`: operational либо permanent |
| кэш wb-серверов | `WbServersCache` | файл `.wb-loader-servers.json`: имена инстансов для автодополнения `--server` |
| прямой sid-режим | `DirectSid` | адресация по явному sid (UUID-форма / `--sid`) без резолва клиента |
| curl-сниппет | `CurlSnippet` | вывод `--print`: эквивалентный curl без выполнения, копия в буфер |
| релог | `Relog` | повторная фиксация перемещения в ту же колонку: PATCH в соседнюю колонку и обратно |
| журнал перемещений | `MoveLog` | локальный журнал перемещений карточек в кэш-БД; пополняют `move`/`ready`/`review`/`close` |
| вид поля | `FieldKind` | закрытый список скалярных полей карточки: `mr`, `hypothesis`, `done`, `result` |
| кастомное поле | `CustomProperty` | кастомное поле карточки Kaiten; ключ в PATCH-теле — `id_<NNN>` |
| токены адресатов | `recipientTokens` | значения `--to`, разбитые по пробелам и приведённые к `@handle`, до раскрытия `@all` и дедупа |
| артефакт | `Artefact` | файловое поле карточки «9. AI-артефакт» (id 610303); заполняется md-вложением |
| чек-лист | `Checklist` | чек-лист карточки Kaiten: имя + упорядоченные пункты; единственные интерактивные чекбоксы |
| пункт чек-листа | `ChecklistItem` | пункт чек-листа: текст, отметка выполнения, дробный порядковый вес |
| ссылка на пункт | `ItemRef` | адрес пункта в аргументах: id пункта либо уникальная подстрока его текста |
| владелец карточки | `Owner` | owner карточки Kaiten (заказчик); цель раскрытия `@all` |
| адресат | `Recipient` | токен `@handle` первой строки комментария |
| вложение | `Attachment` | файл, прикреплённый к комментарию карточки |
| карточка списка | `CardSummary` | элемент выдачи списка карточек Kaiten: состав полей у́же полной карточки и отличается от неё (доска/дорожка id'шниками, без описания, свойств и чек-листов) |
| комментарий | `Comment`, `CommentId` | комментарий карточки Kaiten; текст — GFM markdown |
| файл карточки | `CardFile`, `FileId` | файл карточки Kaiten: уровня карточки, комментария либо кастомного поля (имя `File` занято глобальным типом среды) |
| участник | `Member` | пользователь в составе карточки Kaiten: участники, владелец, автор комментария |
| перемещение | `LocationChange` | запись истории перемещений карточки: колонка и дорожка, автор и момент смены |
| запись времени | `TimeLog`, `TimeLogId` | запись учёта времени на карточке Kaiten; единица длительности — целая минута (wire-поле time_spent) |
| таймер | `Timer`, `TimerId` | личный таймер пользователя Kaiten на карточке; источник истины — поле timer в GET /cards/{id} |
| длительность | `Duration` (`parseDuration`) | целые минуты 1..1440; принимаемые формы ввода перечислены спекой `kiten-time.md` |
| ось обновления | `EditAxis` | поле записи времени, названное вызовом `time edit`; в тело PATCH уходят только названные |
| запись глазами вывода | `TimeLogView` | плоская форма записи времени со снейк-кейс-ключами; она же — JSON-вывод `time ls` |
| чек-лист глазами вывода | `ChecklistView` | чек-лист в форме вывода `ls`: имя, id, отсортированные пункты, счётчик отмеченных |
| пункт с его чек-листом | `CardChecklistItem` | результат сквозного поиска пункта по карточке: сам пункт и чек-лист, в который уйдёт отметка |
| московский день | `mskDay` | календарный день по UTC+3 для любого момента; умолчание даты записи времени. Часы-минуты того же сдвига — `mskClock`, сам сдвиг — `MSK_OFFSET_MINUTES` |
| подсказка таймера | `TimeHint` | локальная строка кэш-БД (одна на карточку): id таймера, роль, комментарий, метка старта, момент последнего списания; эвристика, всегда перепроверяется сервером. В переносе не воспроизводится — локального состояния у команды нет |
| таймер | `Timer` | личный таймер карточки (поле `timer` полной карточки); один на пользователя во всей компании, роли не хранит |
| натёкшая длительность | `elapsedMinutes` | минуты от метки старта таймера до «сейчас», округление вверх |
| фактическая длительность | `factMinutes` | натёкшее на момент остановки; печатается как «по факту», когда `--time` с ним разошёлся |
| границы записи остановки | `StopSpan` | пара `startedAt`/`finishedAt`, уходящая серверу при остановке таймера |
| вторая форма подсказки | `advice` | императив с командами в обратных кавычках, когда действий несколько; одна команда — `hint` (`; попробуй: …`) |
| webapp | `Webapp` | Apps Script-прокси Google Sheets; единственный сетевой канал области sheet (POST {action, …} на WB_PLUS_WEB_APP_URL) |
| экшен webapp | `Action` | имя операции webapp в теле POST (`spreadsheets/values/batchGet`, `spreadsheets/get`, …) |
| кэш листов | `SheetCache` | whole-tab кэш содержимого листов в кэш-БД (таблица sheet_tabs); единица кэширования — лист целиком |
| метаданные листа | `SheetInfo` | title/sheetId/index/rows/cols листа из экшена spreadsheets/get; кэшируются ключом sheet:info:<ss_id> |
| режим рендера | `RenderMode` | слой значений sheet get: both \| values \| formulas \| formatted |
| вид резолва цели | `ResolveKind` | как разобрано значение цели-spreadsheet: url \| id \| alias \| client_id \| title_fuzzy (поле kind в выводе resolve) |
| скрипт мини-языка | `Script` | текст из инструкций batch-мини-языка (из -e/--from/stdin), компилируется целиком |
| инструкция | `Statement` | одна единица скрипта (строка или ;-сегмент); номер инструкции — N в префиксе ошибок «строка N:» |
| глагол | `Verb` | ведущее слово (или пара слов) инструкции записи: set, label, cols insert, … |
| стиль-флаг | `StyleFlag` | токен оформления (bold, bg=#hex, fmt=…) → userEnteredFormat + fields-маска |
| условие | `Condition` | токен условия validate/cond add (num>=N, one-of=…, blank…) → BooleanCondition Sheets API |
| аспект | `Aspect` | sheet-level срез структуры в read-языке batch-get (merges, cond, protected, …) |
| план чтения | `ReadPlan` | результат компиляции batch-get: параметры values/batchGet и/или аспекты+листы для spreadsheets/get |
| грид-диапазон | `GridRange` | каноническая форма диапазона Sheets API: sheetId + 0-based полуоткрытые границы, открытые опущены |
| диапазон размерности | `DimensionRange` | диапазон столбцов/строк Sheets API: sheetId, dimension COLUMNS\|ROWS, 0-based полуоткрытые индексы |
| MR (merge request) | `MergeRequest` | GitLab merge request; адресуется project!iid, iid — внутрипроектный номер |
| MR-селектор | `MrRef` | строка адресации MR: URL \| 'group/repo!iid' \| голый iid \| пусто (автодетект из ветки cwd) |
| проект GitLab | `Project` | путь group/repo; в REST-путях URL-encoded (/ → %2F) |
| дискуссия (тред) | `Discussion` | тред комментариев MR, id — 40-hex; general-тред — без привязки к строке |
| нота | `Note` | одно сообщение треда; системные (system=true) исключаются из ревью всегда |
| позиция | `Position` | привязка инлайн-комментария к строке диффа: 3 SHA + old/new path + old/new line, только form-urlencoded |
| дифф файла | `FileDiff` | изменение одного файла MR: old/new path, unified diff, флаги new/renamed/deleted (статус A/D/R/M) |
| строка диффа | `DiffLine` | строка unified diff: added/removed/context + номера на old/new-сторонах |
| сторона диффа | `Side` | new (правая колонка диффа GitLab) / old (левая); адресация строки комментария |
| ветка пайплайна | `PipelineBranch` | одна из шести веток деплой-конвейера (trunk/main/dev/qa/predprod/prod) — фиксированные колонки таблицы glab-status |
| landing-коммит | `LandingCommit` | коммит, которым MR попал в целевую ветку: первый непустой из merge_commit_sha → squash_commit_sha → sha; по нему определяются галочки веток |
| прочие ветки | `OtherBranches` | ветки с landing-коммитом вне шести веток пайплайна и без source-ветки самого MR; null = данных нет (не запрашивали/недоступно), [] = запросили, пусто |
| диалог | `Dialog` | строка выдачи `telegram ls`: id, title, kind (user\|bot\|group\|channel\|unknown), username |
| маркированный id | `MarkedId` | id чата в клиентской конвенции Telegram: канал/супергруппа −(10^12+raw), базовая группа −raw, пользователь/бот raw; пригоден как адресат |
| строка сессии | `SessionString` | сериализованная MTProto-сессия пользователя в env `TELEGRAM_SESSION`; пишется `mpu init` |
| 10X | `X10` | web-платформа (sw-back API); admin-доступ через staff-аккаунт, база URL X10_URL c авто-суффиксом /api |
| воркспейс | `Workspace`, `WorkspaceId` | воркспейс 10X; инвариант workspace.id == client_id (user.id != workspace.id) |
| impersonation | `Impersonation` | вход в 10X от лица клиента через staff-права; создание сессии пишет audit-запись на проде |
| 10X-сессия | `X10Session` | кэшированный Bearer-токен 10X (вид staff\|impersonation, субъект — логин-email\|user.id); TTL = exp из JWT минус 60 с, fallback 600 с |
| staff-поиск | `StaffSearch` | поиск цели impersonation в 10X: GET /users/staff/search?query&scope; названием клиента/кабинета не ищет |
| scope staff-поиска | `Scope` | трактовка селектора 10X-резолва: auto\|user\|access; auto → access для целого и полного uuid sid, иначе user |
| email-кэш 10X | `EmailClient` | локальная строка кэша email → owned client_ids + сырые воркспейсы (таблица x10_email_clients, PK email в нижнем регистре) |
| owned-воркспейс | `OwnedWorkspace` | воркспейс, где цель impersonation — владелец (ownerId == user.id); источник owned client_id |
| проекция | `Projection` | режим вывода mpu search: одно поле результата голыми строками (null → пустая строка, список sid — CSV); не больше одного флага |
| inner-команда | `InnerCommand` | команда sl-back CLI `node cli service:<сервис> <метод> [флаги]`, собираемая node-CLI обёрткой |
| режим обёртки | `WrapMode` | способ доставки inner-команды: exec через Portainer \| печать ssh-формы \| печать локальной формы; выбирается флагами --print/--local |
| безопасный токен | `SafeToken` | whitelist-валидация значения флага inner-команды (только `A-Za-z0-9 _ . / : - , @ [ ]`); нарушение — exit 2 до сети и печати |
| фрейм | `Frame` | фрейм доски Miro; единица идемпотентного рендера d2-miro (одноимённый удаляется и пересоздаётся на прежнем месте) |
| доска Miro | `MiroBoard` | доска Miro (env MIRO_BOARD_ID / --board); отличать от Board — доски Kaiten |
| shared-таблицы | `SharedTables` | фиксированный список из 18 общих справочных таблиц PG-схемы shared, переносимых copy-shared |
| dt-host | `DtHost` | локальный контейнер переноса данных (compose.sl-dt-host.yaml, `exec cli`); исполняет перенос shared-таблиц и клиентов |
| локальный стенд | `LocalStack` | локальный docker dev-стек (mp-config-local + local-stack): core sl-0/sl-1/nats/nginx/dt-host + web; единственный допустимый target записи копий, host всегда 127.0.0.1 |
| compose-стек | `Stack` | набор env-файлов/compose-файлов/сервисов для одного вызова docker compose up куска стенда (mp-nats, sl-0, sl-1, mp-nginx, dt-host, sw-back-deps); порядок core-стеков — упорядоченная последовательность |
| keep-лист | `KeepList` | инверсный список client_id, которые clean-local-clients оставляет (дефолт 54,776); схема shared сохраняется всегда неявно |
| ход клиента | `Move` | запись «source → target» последнего переноса клиента в кэш-БД (таблица client_moves, PK client_id — одна запись на клиента); пишет move-client, читает/удаляет move-client-back |
| job переноса | `TransferJob` | постановка clientsTransfer createJob (--destroy всегда) в контейнере mp-dt-cli; реальный перенос исполняют внешние воркеры, команда отслеживает только постановку |
| проводка входа | `LoginSeed` | идемпотентный посев user+workspace+кабинеты+подписки в БД воркспейсов для входа в локальный sw-front (email client_<id>@local.host, workspace.id == client_id); best-effort шаг copy-client, снимается clean-local-clients только по своей email-сигнатуре |
| public-строки клиента | `PublicRows` | строки public-таблиц клиента на инстансе (clients, *_loader_info*, clients_modules, spreadsheets и её дети по spreadsheet_id), сопровождающие схему при копии и очистке |
| токен-строки | `TokenRows` | clients/wb_tokens/clients_wb_cabinets на main (sl-0) — authoritative store токенов, копируются отдельно от инстансовых public-строк |
| снапшот тулов | `toolsSnapshot` | текст `tools-<профиль>.json`, собранный из реестра и закрытого списка; сверяется инвариантом, пересобирается задачей `tools:snapshot` |
| обычный файл | `readRegularFile` | чтение файла, где отсутствие пути и каталог на его месте неразличимы для вызывающего: обе ветви — одна ошибка ввода |
| адресат Telegram | `Peer` | цель операции после приведения строки пользователя: `me`, маркированный id, имя пользователя, ссылка t.me, телефон |
| отправленное сообщение | `SentMessage` | результат отправки: `id`, `chatId`, `date` (или `null`); при альбоме — последнее сообщение группы |
| вложение | `Attachment` | файл, уходящий документом без превью; несколько вложений одного вызова образуют альбом |
| настройки прокси | `ProxySettings` | разобранный URL `TELEGRAM_PROXY`: вид туннеля, host, port, учётные данные |
| план закрытия | `ClosePlan` | что `close` собирается сделать; строится целиком до первой мутации |
| план переноса | `MovePlan` | цель переноса, признак релога и положение «до» |
| итог переноса | `MoveOutcome` | положения «до» и «после» плюс перечитанная карточка |
| строка журнала перемещений | `MoveRecord` | одна строка `MoveLog` |
| место карточки | `CardPlace` | доска, колонка, дорожка — из них строится положение |
| поле к записи | `FieldWrite` | вид поля и значение, которое уйдёт на сервер |
| вид скалярного поля | `FieldKind` | ось `--done`/`--result`/… и её id поля инстанса |
| план отправки | `SendPlan` | разобранный ввод одного вызова отправки: адресат, текст, разметка, вложения |
| исходящий документ | `OutgoingDocument` | вложение, уходящее в Telegram; подпись несёт только последнее |
| сообщение клиента | `ClientMessage` | сообщение, как о нём отчитался клиент, до проверок: идентификатор чата и время могут отсутствовать |
| опознанный адресат | `PeerRef` | адресат, уже опознанный клиентом Telegram |
| вид туннеля | `ProxyTunnel` | http-CONNECT либо SOCKS после приведения синонимов схем |
| конфигурация Telegram | `TelegramConfig` | значения сеанса из env-файла |
| сеанс Telegram | `TelegramSession` | открытый сеанс: клиент отправки и его закрытие |
| догадка об имени | `guess` (вид адресата) | голая строка, читаемая и как имя пользователя, и как название чата: резолвится в две попытки. Объявленный вид (`@`, ссылка t.me) — `name`, второй попытки не имеет |
| найденное сообщение | `FoundMessage` | строка выдачи `telegram search`: id, чат (id и название), отправитель, дата, текст, ссылка |
| найденное сообщение до приведения | `RawMessage` | ответ клиента о сообщении: чат и отправитель сырыми, время объектом даты |
| план поиска | `SearchPlan` | что и где ищем после разбора ввода: режим, адресаты, лимит |
| адресат со строкой пользователя | `PeerTarget` | разобранный адресат вместе с исходной строкой — её называет текст отказа |
| потолок скана | `SCAN_CAP` | 1000 просмотренных сообщений клиентского фильтра `--from` без `--chat` |
| перемещение карточки в отчёте | `CardMove` | карточка, заголовок, url, колонка и момент — общая форма для журнала и живого опроса |
| строка журнала для сводки | `LoggedMove` | пять полей строки журнала перемещений, нужных дневному отчёту |
| окно московского дня | `DayWindow` | день и его границы: в секундах эпохи для журнала, в ISO UTC для запроса |
| стиль отчёта | `ReportStyle` | замены имён колонок и переопределения эмодзи из env-файла |
| источник живого опроса | `LiveSource` | узкий интерфейс каталогов Kaiten, из которого дневной отчёт берёт данные |
| таргет вызова до выбора транспорта | `ExecPlace` | сервер, dev-нода или контейнер по имени — то, что назвал пользователь |
| готовый адрес исполнения | `ExecTarget` | контейнер, бэкенд и его доступы: всё, что нужно транспорту |
| override транспорта | `Via` | значение `--via`: ssh либо portainer |
| строка кэша про контейнер | `ContainerLocation` | имя контейнера вместе с базой Portainer и endpoint'ом |
| пара «база Portainer + endpoint» | `PortainerLocation` | адрес, по которому исполняется exec |
| приёмник вывода удалённой команды | `RemoteOutput` | куда транспорт отдаёт stdout/stderr по ходу исполнения; порт — `openRemoteOutput` |
| запуск локального процесса | `RunProcess` | порт запуска подпроцесса (ssh-бэкенд) |
| байтовый канал до сервера | `ByteChannel` | соединение, поверх которого идут кадры; открыватель — `OpenChannel` |
| кадр WebSocket | `WsFrame` | единица обмена стрима Portainer; имя `Frame` уже занято фреймом доски Miro |
| подписка на Ctrl+C | `OnInterrupt` | порт прерывания: по нему транспорт убивает удалённый процесс |
| хук Claude Code | `claudehook` | внешняя команда, вызываемая Claude Code на событии сессии: payload приходит JSON-объектом на stdin, а вывод и код выхода у события `Notification` игнорируются |
| конверт события | `HookPayload` | разобранный JSON-объект payload'а хука без привязки к конкретному событию: разбирается общим кодом, поля читает уже то, что специфично для события |
| matcher | — | поле настройки хука в `~/.claude/settings.json`, отбирающее типы событий; единственное место отбора — фильтра типов у команды нет |
| рабочая область | `Workspace` | каталог, чьи подкаталоги первого уровня с `.git` — репозитории, по которым отвечает `mpu code` |
| репозиторий рабочей области | `Repo` | один такой подкаталог со своим корнем и своей отметкой дерева |
| проект | `Project` | конфигурация вне `node_modules`/`dist` с непустым списком файлов: `tsconfig.json` либо `deno.json`/`deno.jsonc`; единица разбора |
| анализатор | `Analyzer` | разбор одного репозитория: отметка, объявления, потребители, тела |
| гарантия ответа | `Guarantee` | полнота, которую даёт анализатор: разбор по типам либо текстовый |
| отметка дерева | `TreeStamp` | репозиторий, ветка, коммит, признак незакоммиченных изменений; `вне git` — тоже отметка |
| адрес в коде | `CodeAddress` | `[РЕПОЗИТОРИЙ:]ПУТЬ[:СТРОКА]`, разобранный один раз на входе |
| цель запроса | `Target` | что спрашивают: символ в строке либо модуль целиком |
| потребитель | `Consumer` | файл, получающий символ (импортом или реэкспортом); единица перечня — файл, не обращение |
| неразрешённая ссылка | `Unresolved` | импорт, который не удалось разрешить; печатается разделом, а не выпадает |
| нормализованная форма тела | `BodyShape` | тело без комментариев, со сжатыми пробелами, переименованными локалами и позиционными литералами; по ней ищутся похожие |
| вход проекта | `ProjectEntry` | файл из поля входа пакета (`exports`/`main`), иначе `index.ts` в общем корне исходников; по нему различаются два уровня экспортности |
| область видимости | `Scope` | где символ виден: из входа проекта, только из модуля, приватный. Четвёртого значения нет: объявлений текстовый разбор не выдаёт вовсе |
| читатель | `Reader` | файл, импортирующий модуль; ответ на цель-модуль — не то же, что потребители символа |
| оракул полноты | — | эталон проверки: переименовать символ и посчитать сломавшееся; перечень команды обязан совпасть с ним |
| запись | `Entry` | одна найденная вещь раздела: потребитель, тело, объявление, упоминание. `--limit` и счётчик усечения считают записи, а не строки: у `name` запись занимает две строки |
| раздел ответа | `Section` | часть ответа, относящаяся к одному репозиторию: своя отметка и свой исход |
| отказ раздела | `SectionRefusal` | раздел, который не ответил: печатается вместо перечня, гарантии у него нет |
| соседи по сигнатуре | `SignaturePeers` | объявления с тем же списком типов параметров и другим именем |
| строка существования | `ExistenceLine` | первая строка ответа `mentions`: есть путь в коде или нет |
| вхождение | `Occurrence` | пара «документ, строка» в ответе `mentions` |
| программа | `Program` | построенный компилятором разбор конкретного состава файлов: проект даёт состав, программа даёт ответы. Звучит в тексте отказа «ни одна программа не собралась непустой» |
| стек MR | `stackedOn` | MR стоит на другом открытом MR: ветка второго — предок ветки первого и ещё не влита в его целевую |
| файлы конфликта | `conflictFiles` | пути, которые `git merge-tree` целевой и исходной ветки называет в `CONFLICT (…)`; без слияния |
| сообщение | `Message` | одно звено строки вызова: унарное (одно слово) или ключевое (пары «ключ: значение» / `--ключ значение`); платформа — `platform/messages.md` |
| приёмник | `Receiver` | объект, которому отправлено сообщение; первый — корневой объект `mpu`, следующий — результат предыдущего |
| селектор сообщения | `MessageSelector` | имя сообщения: слово унарного или набор ключей ключевого. Не путать с «селектором» — строкой адресации клиента |
| цепочка | `MessageChain` | все сообщения одной строки по порядку; объекты-результаты живут в её пределах |
| не понято | `doesNotUnderstand` | ответ приёмника на сообщение без своего селектора; коллекция переопределяет его поиском элемента |
| вид звена | `LinkKind` | имя, которым звено с произвольным словом входит в путь правила (`<card>`, `<client>`) |
| объект «значение» | `ValueReceiver` | результат-данные в конце цепочки; на любое следующее сообщение отвечает «цепочка окончена» |
| правило подтверждения | `ConfirmRule` | шаблон пути сообщений → решение; хранится в SQLite. Не путать с «политикой» `ro`/`rw`, которая уходит после переключения |
| решение | `Verdict` | исход проверки правил: выполнить (`allow`), спросить (`ask`), отказать (`deny`) |
| наследуемое правило | `InheritedRule` | ответ для пути без своего правила: решение ближайшего предка |
| отказ клиента Telegram | `clientRefusal` | отказ протокола, библиотеки, криптографии или уже оформленный слоем; прочее — сбой вне Telegram, уходит тем же объектом |
| граница порта сеанса | `refusing` | место, где отказ вызова клиента Telegram проходит классификатор отказа клиента |
| рабочая область | `workspace` | дерево `ts/` из нескольких самодостаточных подпроектов; состав — корневой `deno.jsonc` |
| подпроект | `back`, `mcp`, … | папка `ts/<имя>/` со своим `deno.jsonc`, тестами и гейтами |
| признак дерева исходников | `ts/main.ts` | файл без кода: по паре `deno.jsonc` + `main.ts` установленный бинарь узнаёт дерево (`specs/build.md`) |
| слово строки | `Word` | одно слово строки вызова как объект: ключ, флаг, точка, `--`, `--help` или обычное слово |
| вид ключа | `KeyKind`, `Kind` | берёт ли ключ значение (`value`) или это флаг (`flag`) |
| черновик сообщения | `Draft` | ключевое сообщение, пока шаг разбора набирает его пары |
| шаг разбора | `readMessage`, `MessageStep` | слова + описание приёмника → одно сообщение и остаток слов |
| вид объекта | `Shape` | словарь методов объекта, как класс: знает свои селекторы и справку без создания объекта |
| объект | `Instance` | вид и собственное состояние |
| вид результата | `ResultKind`, `Yields` | что возвращает метод: данные (`DATA`) или объект какого вида |
| метод вида звена | `LinkMethod` | ответ объекта на произвольное слово (номер карточки, текст) |
| режим справки | `Mode` (`NORMAL`/`HELP`) | строка с `help`: последнее сообщение не исполняется, итог — справка его метода |
| отказ | `Refusal`, `Rejection` | отказ объекта; итоговый отказ строки с путём спереди, код 2 |
| заменяемый метод | `Replaceable` | метод из модуля по URL; новая версия действует со следующего сообщения |
| хвост | `tail`, `<args>` | остаток строки, забранный приёмником как есть: слова уходят нынешнему разбору аргументов команды |
| мост реестра | `back/src/next` | дерево объектов из записей реестра; исполняет узел нынешняя диспетчеризация пути |
| путь правила | `RulePath` | звенья пути через пробел; корень — `*`; хвостовое ` *` отбрасывается |
| набор правил | `Rules` | решение для пути: самое длинное совпавшее правило, иначе наследуемое (`INHERITED`, решение `ask`) |
| файл правил | `RuleBook`, `policy.db` | SQLite в каталоге состояния; перечитывается перед каждым решением, если его изменил другой процесс |
| посев | `seed`, таблица `seeded` | первое правило для пути, которого посев ещё не видел: `ro` → `allow`, `rw` → `ask` |
| канал вызова | `Channel` (`Human`, `NOBODY`) | кого спросить о подтверждении: человек в терминале или никто |
| сервер строк | `mpu-back`, `back/src/backend` | долгоживущий процесс: строки по WebSocket, запросы `/rpc`, снимок дерева |
| дверь | `Door` (`HUMAN_DOOR`, `AGENT_DOOR`) | путь подключения `/line` или `/agent/line`; задаёт канал вызова |
| канал агента | `Agent` | канал, где вопрос `ask` задаётся, а изменение правил (`amend`) всегда отказывает |
| кадр строки | `LineRequest`, `ServerFrame` | JSON-объект WebSocket: первый кадр клиента, `out`/`err`/`ask`/`exit` сервера, `answer` клиента |
| снимок дерева | `TreeNode`, `tree.json` | узлы дерева `mpu-next`: путь, назначение, селекторы, хвост |
| тонкий клиент | `cli`, `runClient` | `mpu-next` в терминале: отдаёт слова серверу строк, печатает кадры, спрашивает человека |
| агентский токен | `agent-token` | второй токен сервера строк: только `/agent/line` и `/rpc`, `human` с ним всегда `false` |
| кто пришёл | `Caller` (`OWNER`, `AGENT`) | чей токен у запроса; решает, верить ли `human` из кадра |
| дверь клиента | `Door`, `chooseDoor` | путь и токен, выбранные клиентом один раз по доступным токенам и терминалам |
| строка простым HTTP | `POST /line`, `http.ts` | строка без WebSocket: кадры потоком NDJSON или один собранный JSON |
| номер подтверждения | `ticket`, `Tickets` | одноразовый номер вопроса: 120 секунд, та же дверь и тот же токен |
| доставка кадров | `Delivery` (сокет, NDJSON, собранный, `DETACHED`) | как строка отдаёт кадры своему клиенту |
| способ спросить | `Asking` (кадром, номером) | как строка задаёт вопрос: в том же сокете или номером с концом ответа |
| переводчик MCP | `mpu-mcp`, `ts/mcp/` | MCP-сервер с тулами `help` и `mpu`; строки — в `POST /agent/line`, вопросы — elicitation |
| токен клиента MCP | `mcp-token` | токен, которым Claude Code входит в `mpu-mcp`; в конфиг клиента не пишется, отдаётся командой `headersHelper` |
| собранный ответ | `Collected`, `collectedOf` | ответ строки одним JSON: потоки и код либо вопрос с номером |
| супервизор | `mpu-supervisor`, `Supervisor` | единственный процесс службы `mpu-next`; держит `mpu-back` и `mpu-mcp`, перезапускает их по одному |
| дочерний | `Child` (`CRASH`, `RESTART`, `STOP`, `NO_PROCESS`) | процесс под супервизором со своей паузой падений |
| установка | `install.sh`, `MPU_OUT`, `MPU_DENO` | сборка, сравнение по sha256, подмена изменившегося, служба, проверки |
| дополнение | `mpu-complete`, `ts/complete/` | варианты строки для bash, fish, nu из снимка дерева; без сети и токенов |
| флаги узла снимка | `flags`, `commandFlags` | флаги команды из её объявления (длинные, короткие, `--json`) |
| назначения селекторов | `summaries` | назначение собственного селектора без своего узла (`allow:`, `policy`…) |
| контекст вызова | `CallContext`, `callContextOf` | что принёс вызывающий: ввод, терминальность, переменные; живёт ровно одну строку |
| ввод строки | `LineInput`, `NO_INPUT` | весь stdin значением; повторное чтение даёт то же |
| терминальность клиента | `Terminals`, `NOT_TERMINALS` | три признака терминала и ширина консоли вызывающего |
| правило окружения | `EnvRule`, `SERVER_RULE` | чьи переменные видит строка: только сервера или клиента поверх сервера |
| закрытый список имён окружения | `CLIENT_ENV_NAMES` | имена, принимаемые от клиента (только про вид вывода) |
| ширина консоли | `consoleColumns` | порт `CommandIo`: сколько знакомест у консоли вызывающего |
| каталог вызова | `Workdir` | каталог, от которого строка разрешает относительные пути; свой у каждой строки |
| предел одновременности | `Lines`, `--lines`, `DEFAULT_LINES` | сколько строк сервер исполняет разом (16 по умолчанию) |
| место в пределе | `Slot`, `NO_SLOT` | занято строкой до её кадра `exit`; отпущенное подменяется null-объектом |
| параметры запуска подпроцесса | `ProcessRun` | что подать, куда деть вывод и в каком каталоге стартовать |
| просьба остановиться | `Stopping`, `CommandIo.signal` | как строка узнаёт, что её больше никто не слушает |
| код отменённой строки | `CANCELLED_CODE` | 130: строку остановили, а не она кончилась сама |
| готовность доставки | `Delivery.ready`, `Readiness` | клиент разобрал отданное и готов принять следующий кадр |
| срок до снятия | `KILL_AFTER_MS`, `ProcessRun.killAfterMs` | сколько подпроцессу дают уйти самому между `SIGTERM` и `SIGKILL` |
| вид вопроса | `AskKind`, `kind` | `line` — видимый ответ, `secret` — скрытый; агенту `secret` не задаётся |
| просьба о буфере обмена | кадр `clip` | сервер просит клиента положить текст в буфер; у пайпа — в stderr |
| получатель копирования | `Clip`: `clipboard`, `shown` | кто исполняет просьбу у клиента |
| исход вопроса | `Answer<T>`: `given`, `absent` | ответ получен либо спросить было некого |
| переключение имён | `cutover.sh` | перенос имени `mpu` с монолита на тонкого клиента; скрипт временный, до порции 15b |
| блок дополнения | `# >>> mpu completion >>>` … `# <<< mpu completion <<<` | текст в файле настроек оболочки, принадлежащий установщику целиком: он его заменяет, а не дописывает |
| задача сборки монолита | `compile:monolith`, `back/scripts/compile_task.ts` | чем собирается монолит для прогона `smoke`; имя задачи названо в одном месте |
| предмет прогона | `smoke.ts` | пара собранных программ — сервер и клиент, — на которых проверяются зашитые права |
| разрешённые строки прогона | `ALLOWED` | строки, которым прогон засевает решение `allow` до старта сервера: умолчание «спросить» он не прячет |
| прямое исполнение | `immediately`, `directFrames` | исполнение строки без двери: так ходят прогоны равенства, в работе такой конфигурации нет |
| исполнение строки | `lineEntry`, `LinePorts`, `back/src/line/` | точка, через которую строка исполняется, и её порты |
| уход клиента | `Opened.leave`, `Client.lost` | обрыв запроса, который форма ответа толкует сама: ответ ещё не отдан — значит ушёл |
| 499 у оборванного ответа | `http.ts` | чем собранная форма закрывает запрос ушедшего: обработчик без ответа оставил бы запрос висеть |
| взгляд | `View` (`NORMAL`, `DOOR`), `back/src/line/view.ts` | как дерево показывается и исполняется с данного адреса: обычный (`mpu …`) и дверь (`mpu ask …`) |
| дверь `ask` | `DOOR`, `ASK_DOC` | взгляд, через который исполняются строки с решением `ask` |
| вход | `gate`, `GateCall`, `Trace.gate` | слово адреса строки, не звено пути правил (`ask`) |
| адрес строки | `Address` (`onAllow`, `onAsk`) | у кого решение правил спрашивает исход |
| исход у адреса | `Treatment` (`EXECUTE`, `CONFIRM`, `REDIRECT`) | исполнить, спросить человека или отказать с подсказкой адреса |
| роспись | `Roster`, `EVERYONE` | кого вид называет в списках (справка, «ближайшие»); поиск и разбор роспись не трогает |
| адресный отказ | `Execution.redirect` | «строка не по адресу — вызывай …», код 2, до вопроса и исполнения |
| слова грамматики | `GRAMMAR` (`open`, `close`, `literal`), `back/src/messages/words.ts` | `do`, `end`, `--` — одна константа; разбор, справка, описание тула берут их отсюда |
| закрытие / неявное закрытие | `{ close: true }`, `Words.closeHere` | `end` и унарное за литеральным значением: дальше — сообщение результату |
| объект-справка | `Help`, `back/src/objects/help.ts` | ответ `help`: данные (`path, purpose, text, examples, keys, formats, messages`) и их вид |
| результат команды / строка к исполнению | `ResultOf`, `Pending`, `back/src/line/result.ts` | форматы — методы результата; неизвестный — отказ до исполнения |
| ключевая команда | `Keys`, `KeyCall`, `back/src/line/keyed.ts` | лист, принимающий значения только ключами; снятый вход — `retired` |
| остаток ключевого сообщения | `Leftover` | часть склеенного ключевого, которую понимает не команда, а её результат |
| слово в стороне | `aside` | вход, `end`, формат — в адресе строки, не в звеньях правил |
| подсказка отказа | `Remedy`, `NO_REMEDY` | готовая исправленная строка в тексте отказа |
| печать записи | `EntryPrinter`, `JSON_LINES` | вывод потока по мере поступления |
| каталог ключей | `Keys`, `back/src/line/keys.ts` | один источник ключей команды: разбор, справка, строка прежней диспетчеризации, тест полноты |
| снятое написание | — | короткий флаг, snake_case, прежнее имя переименованного входа, флаг формата: отказ с готовой строкой |
| режим команды | `CommandMode` (`fixed`, `keys`) | унарное сообщение листа, задающее значения позиционных входов (`logs hosts`) |
| раскладка строки | `Layout` (`BY_PATH`, `SELECTOR_AHEAD`) | как ключи собираются в строку прежней диспетчеризации |
| причина имени ключа | `why`; «прежнее имя входа» | почему ключ вне словаря |
| печатаемая строка для вставки | — | строка, которую команда печатает для копирования; обязана разбираться новым разбором |
| протокол отражения | `Reflection`, `back/src/objects/reflection.ts` | `messages`, `understands:`, `keys`, `formats`, `variants`, `candidates:` — одна реализация на все объекты |
| ответ протокола | `Listing` | текст по строке на элемент, `end json` — данные |
| значения ключа | `candidates:`, `Values`, `targetValues` | подходящие значения ключа (`target` — из кэш-БД) |
| дополнение строки | `complete:`, `completeLine` | кандидаты следующего слова тем же разбором, без исполнения |
| значение разбора | `Literal`, `Flag`, `GroupValue`, `StdinValue`, `ListValue`, `back/src/messages/value.ts` | что стоит на месте значения ключа; вычисляется до отправки сообщения |
| виды данных | `Collection`, `Row`, `Scalar`, `NIL`, `Data`, `back/src/objects/data.ts` | коллекция, запись, скаляр; `nil` — один null-объект |
| отбор | `Selecting`, `Source`, `Ended` | сообщения отбора над исполненным результатом, без повторного запроса |
| коллекция команды | `items` (`Items`) | объявление команды: результат → записи и обратно |
| доставка результата | `Delivery` (`PRINT`, `Taking`), `ResultEnd` (`SELECTABLE`, `FORMATS_ONLY`) | куда идёт результат строки: печать, значение, отбор; поток — только форматы |
| вызывающий | `caller`, `Naming`, `NAMELESS` | кто прислал строку: `ppid:` (CLI), `mcp:` (сессия MCP), `web:` (хэш сессии, называет `back`) |
| память результатов | `LastResults`, `Memory`, `NO_CALLER`, `Keeper` | последний результат-данные на вызывающего, в памяти `back` |
| прошлый результат | `it`, `Recalled`, `Absent` | сообщение корню: последний результат вызывающего без повторного исполнения |
