/**
 * Признак дерева исходников, а не точка входа. Установленный `mpu`
 * узнаёт дерево по паре `deno.jsonc` + `main.ts` в его корне
 * (`docs/specs/build.md`), и бинарь, собранный до переезда реализации
 * в `back/`, другого признака не знает. Точка входа `deno compile` —
 * `back/main.ts`.
 */
