/**
 * Имена прежней реализации, которые решено не переносить
 * (`platform/registry.md`). Список нужен не коду, а человеку: слепок
 * дерева (`manifest.ts`) остаётся свидетельством того, что в Python-
 * версии было, и без этого перечня разницу «ещё не переехало» и «не
 * переедет никогда» пришлось бы восстанавливать по памяти.
 *
 * Раньше похожий смысл нёс `NOT_LEGACY` в порождении записей маршрута
 * `legacy`, но означал он ровно обратное — «уже перенесено, подпроцессу
 * не отдавать». Маршрут снят целиком (порция 97), и путать эти два
 * списка больше нечему: здесь имена, у которых реализации не будет.
 */

/** Выброшенное имя и причина: причина обязательна, а не комментарий. */
export interface DroppedName {
  /** Путь в слепке: одно имя — семейство целиком. */
  readonly path: readonly string[];
  readonly reason: string;
}

export const DROPPED: readonly DroppedName[] = [
  {
    path: ["iu-wb"],
    reason: "обёртки над `node cli service:iuWb` (sl-back), запускавшиеся " +
      "вручную; переносить не стали (порция 97)",
  },
  {
    path: ["ozon-fix-fo-tax"],
    reason: "разовая починка источника 1₽ с прогоном пересчёта ОПиУ; " +
      "переносить не стали (порция 97)",
  },
  {
    path: ["sheet", "sync"],
    reason: "заменено `mpu update`: кэш `sl_spreadsheets` наполняет он " +
      "(`src/update/cache.ts`), второй команды для того же незачем",
  },
];
