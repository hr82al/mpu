/**
 * Ворота пайпа (`docs/specs/confirm.md`): показать буфер человеку и
 * спросить у терминала, пропускать ли его дальше.
 *
 * Наружу модуль отдаёт только команду реестра; разбор ответа и текст
 * диагностики остаются внутренностями.
 */

export { confirmCommand } from "./cmd_confirm.ts";
