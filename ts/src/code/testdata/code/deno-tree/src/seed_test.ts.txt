// Тестовая обвязка Deno: имя *_test.ts, шаблоном «**/*.spec.ts» не
// описывается вовсе, а потребителем является.
import { addDays } from './days.ts';

Deno.test('следующий день', () => {
  if (addDays('2026-01-01', 1) !== '2026-01-02') throw new Error('bad');
});
